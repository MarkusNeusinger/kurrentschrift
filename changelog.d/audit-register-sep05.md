### Changed

- **The 2026-09-02 audit's register brought up to the 2026-09-05 state.** The
  synthesis note's `Erledigungsstand` table is its one living part — the 39
  findings and their ranked write-up stay frozen. Verified against `gh pr
  view` and the merged commits since 2026-09-03: thirteen more rows close
  (Sperr-Doktrin #513, the campaign journal's move to `messjournal.md` #521,
  the pairlab production-connector re-measure #527, the deploy-api trigger's
  `app/prerender/**` include checked live, the 121 stale branches deleted, the
  Federprobe's three stages #535/#537, the letter-detail example-word bank
  #539, the `/tafel` CLS root cause and fix #522, the privacy wording #507,
  `dev_ids()` reproducibility #510, the lint parking lot and `apiFetch`
  timeout #515/#481), one moves from an open decision to measured-but-not-yet-
  applied (the chart-form class rule and generated d-Auslauf, #530), and the
  proof floor under the Laufform rows turns out to cover four lines today, not
  two (#529). A new dated block lists the four post-audit A-items that came
  out of the same work and never had a row of their own (#534, #536, #532,
  #533, plus their immediate follow-ups #540/#541/#542/#538). Summary counts
  recomputed: 33 done, 5 partial, 1 measured and rejected, none untouched.
