### Added

- **The blind trace round on the K-E mark claim is judged, filed and booked as
  an honest negative.** Round 9 — 54 screens, the shipped production chain v5
  against the same run plus `--mark-claim`, both arms drawn as followed
  centerlines over the plate's own ink, so the accuracy question and the tag
  `VERGLEICH` — returned **no decided screen at all**: 44 of 44 words "no
  difference", 100 % ties against a ≤ 25 % threshold, and a candidate share
  that has no denominator. Three of the four pre-registered falsifiable
  expectations fall. `ziel` was claimed visible FOR the candidate and is not:
  the `die-2` healing the whole family was founded on (Soll 5 → 4, aiou
  +0.0227) is not an eye finding, and the half sentence "the V needle is gone
  by eye" in the `sep07` entry was a builder's construction note, never a human
  verdict. `riss` was claimed visible for the base and is not either: the
  standing −0.003 aiou gate punishes something at `regieren` and `muß` that no
  human sees. Only the null controls hold, 6 of 6 correct — so the answer habit
  is not the explanation.
- **The round's own artefacts are in the repo** (`data/humanbench/runde-09-*`):
  the result text, the narrow key with its suspicion class per screen, the
  analysis JSON (reproduced byte-identically from the full key, checked) and a
  provenance stamp carrying both arm checksums, the root export the round was
  built against and the dirty-worktree flag of its build commit. The full key,
  the payload, the two arm files, the two follower candidates and the strata
  file with its per-word displacements stay out. The stamp says what the narrow
  key can and cannot recompute: everything except the mirroring flag and the
  arm naming, which is exactly what makes "10/10 same arm across mirrored
  repeats" readable.

### Changed

- **The K-E claim family is closed for good, and the lever the round found is
  the SEED, not the claim.** The pre-registration said a No "closes the claim
  family definitively and moves it to rescue path (2)", so `mark_claim` stays
  off and nothing is put to the author. What the round adds is a direction:
  the autopsy over the two arm files shows both of them carrying the same
  dominant defect — zig-zag retrace zones that leave the ink and run over the
  PAPER, 29 : 27 across the 44 judged words, with the claim moving them both
  ways (`Sprünge` 4 → 0 and `die`/`die-2` 1 → 0 heal, `regieren` 4 → 9 and
  `Zügel` 2 → 5 get worse). That is the judge's "schlimm zu schlimm aber
  bischen anders" measured. And it answers the question he asked back: where
  the composed seed lands within the solver's step budget the chain is clean,
  where it misses by more than `max_delta` the solver squashes the path onto
  the wrong ink — diagnosed at `unter` in the `aug20` K-C autopsy and open
  since then as the composer's e-width task, which now has an addressee from a
  human round. Rescue paths, the §7.9 row and the §7.11 arms ship with the
  entry.
- **The word mode's visibility floor is not a property of the displacement
  alone.** `menschliche-bewertung.md` §8a carries that as an observation and a
  BUILD obligation, never as a threshold, and extends round 7's obligation to
  TRACE rounds: five of the judged words move by at least the 0.1186 xh at
  which round 5's strong class was decided at 9.7 % ties — up to 0.3221 — and
  every one of them was called "no difference". The instrument was intact
  while that happened (null probes 6 of 6, 77 s and 74 s spent on the two most
  conspicuous screens against a 3 s median). So a round that calls a class
  visible now names the displacement AND the frame in which that displacement
  would be visible; a round whose two arms carry the same dominant defect asks
  about a difference the display cannot show next to it.
- **The ad hoc sensor behind that autopsy was corrected before it was
  quoted, and the retraction is in the entry.** The first count scored every
  reversal and therefore scored the plate's own ductus — arcade tops, loop
  turns, all inside the ink — which put `Galoppieren` near the top at 23 : 24;
  the author objected that `Galoppieren` looks fine, and the image agrees. The
  corrected count keeps only reversal vertices whose pixel lies in the paper.
  Same lesson as the Kringel catalogue: a sensor that does not know the
  landmarks measures the ductus instead of the defect. It is an autopsy, not a
  ruler, and the entry says so.
