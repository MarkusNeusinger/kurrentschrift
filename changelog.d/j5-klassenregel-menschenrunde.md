### Added

- **The blind word round on the J5 class rule is judged, filed and booked as an
  honest negative.** Round 6 — 38 screens, base against `apex_handover` +
  `stem_depart` with the placement pinned — goes 20 : 1 to the base among the
  21 decided screens, and the sharpest reading is per class: `stem_depart`,
  which had passed every gate on 2026-09-04 and improved both headline numbers,
  loses the eight words where it alone fires by 7 : 0. The five words the word
  ruler had credited to the apex arm all go to the base as well, so the metric
  is measurably rewarding what the eye rejects. Measured out of the round's own
  payloads, next to the judge's note that the up-strokes "were unnaturally
  straight lines": the generated handover runs 1.67–1.84 x-heights with a
  sagitta of 0.001–0.002 xh and deletes an Anstrich that carried 0.015–0.040,
  against the chart cell's 0.136–0.187. Author decision A36 therefore leaves
  `stem_depart` off, which also makes the announced golden re-bake moot — no
  `/write/word` answer moves. Numbers, per-class table and four rescue paths
  are in `messjournal.md` §14 „Übergänge J5 `sep05`", the register row and the
  §7.9 row ship with them.
- **The round's own artefacts are in the repo** (`data/humanbench/runde-06-*`):
  the result text, the narrow key with its suspicion class per screen, the
  analysis JSON and a provenance stamp carrying both arm checksums. The key,
  the payload and the two arm files stay out, as the method requires — the
  judgement is the one part of the chain that cannot be recomputed, so it is
  the part that is kept.

### Changed

- **The word mode now says how many null controls a narrow class needs.** The
  repeat pool is `n − min_gap − REPEAT_JITTER`, so round 6 asked for six blind
  repeats, got a reported four, and could never have carried an adoption claim
  — `menschliche-bewertung.md` §8a carries the arithmetic and the corollary
  that a round supporting the status quo does not need that floor at all. The
  same round closed the doubt left over from the LF11 verdict: twelve
  bit-identical pairs, twelve correct "no difference" answers, so that round's
  tie rate was not an answering habit.
