### Added

- **The `d` running-form row becomes an arm of its own, pre-registered and
  built.** Round 8 threw out the 15-row chart-seeded card as a whole, and its
  per-row decomposition found exactly one row that cleared both thresholds:
  `d` at 10 : 2 of the decided words with 14.3 % ties — the row the judge named
  unprompted. That number was conditional, because all fifteen rows moved at
  once. On the author's decision A40 the row now stands alone: a fresh harvest
  on the production root (twice, byte-identical, 227 occurrences), an overlay of
  that ONE row over the stored rows, and a short human round over the 40 words
  it takes to carry blind repeats. Two byte comparisons keep the round anchored
  to the judged one instead of merely resembling it — the harvested row comes
  out identical to the one round 8 showed (the comma exclusion touched no word
  drawing a `d`), and the base arm's composed words are identical too. The
  pre-registration, the ruler numbers (0.108153 → 0.108339, pairs unmoved) and
  the visibility figure the round is bound to (median `arm_gap` 0.1030 xh
  against a declared 0.05 floor) are in `messjournal.md` §14 „Laufform LF17
  `sep09`"; the write of the row stays the author's step and a declared
  re-baseline, with the script prepared as a dry run by default.
