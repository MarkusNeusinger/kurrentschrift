// PublicHeader — the sticky identity bar for the public pages.
// Two tones; the viridian dot + ".ink" accent are the same in both.
import { PublicHeader } from 'kurrentschrift-app';

export const Paper = () => <PublicHeader tone="paper" />;

export const Plain = () => <PublicHeader tone="plain" />;
