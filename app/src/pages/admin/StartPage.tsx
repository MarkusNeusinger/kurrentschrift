import { StartView } from '@/sections/admin/shell/StartView';

// Thin route mount for /admin — the Vorlage picker the admin area is entered
// through.
export default function StartPage() {
  return <StartView />;
}
