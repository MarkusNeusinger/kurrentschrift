import { JoinView } from '@/sections/admin/joins/JoinView';

// Thin route mount — the section component carries the whole Übergänge view.
export default function JoinsPage() {
  return <JoinView />;
}
