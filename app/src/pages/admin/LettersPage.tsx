import { LetterView } from '@/sections/admin/letters/LetterView';

// Thin route mount — the section component carries the whole Buchstaben view.
export default function LettersPage() {
  return <LetterView />;
}
