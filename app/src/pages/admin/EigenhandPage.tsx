import { EigenhandView } from '@/sections/admin/eigenhand/EigenhandView';

// Thin route mount — the section component carries the whole Eigenhand view.
export default function EigenhandPage() {
  return <EigenhandView />;
}
