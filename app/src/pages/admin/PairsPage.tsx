import { PairMatrix } from '@/sections/admin/pairs/PairMatrix';

// Thin route mount — the section component carries the whole pair matrix.
export default function PairsPage() {
  return <PairMatrix />;
}
