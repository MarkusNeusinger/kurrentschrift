# Vollaudit 2026-09-01/02 — Synthese

> **Status (2026-09-02): Befund-Journal, wird nicht fortgeschrieben.** Synthese über die 20 Prüfberichte des Vollaudits (Rohbefunde: `audit-2026-09-02-rohbefunde.md`). Rangliste, Parallelplan und Owner-Fragen sind der Vorschlag des Synthese-Prüfers; Entscheidungen des Autors stehen nicht hier, sondern in den PRs, die daraus folgen.

## Gesamtbild

Zwanzig Prüfer haben ein Repository vermessen, das handwerklich deutlich besser dasteht, als eine Momentaufnahme der offenen Punkte vermuten lässt: die Doktrin ist nicht nur aufgeschrieben, sondern an den entscheidenden Stellen technisch erzwungen (Korb-Protokoll, öffentlich/reserviert-Pin, Pre-Reg vor der ersten Zahl, Prerender-Drift-Guard), die Deploy-Kette der API ist mit Migrate-Job, Kandidaten-Revision und Smoke besser als in den meisten Häusern, 467 Doku-Links sind tot-frei, und die Website macht auf einen sorgfältigen Erstbesucher einen sehr guten Eindruck — bis er eines der Werkzeuge anfasst. Genau dort liegt der Bruch: drei ausgelieferte Werkzeuge sind kaputt oder unehrlich. `/lesen/vergleichen` hat in Produktion kein Wörterbuch (live nachgeprüft: `readings: []`, `dictionary: null`) und schreibt trotzdem „sie ist wohl eindeutig“ direkt über den Hinweis, dass das Wörterbuch fehlt; „Lesetafel als PDF“ erzeugt seit einer CORS-Cache-Kollision gar kein PDF mehr; das Übungsblatt lässt Vorschrift-Zeilen ab etwa 18 Zeichen kommentarlos weg, obwohl der eigene Hilfetext 60 verspricht. Das ist der erste Hebel und zugleich der billigste: die Seite darf nichts behaupten, was sie nicht kann. Der zweite Hebel liegt beim Geschriebenen — und er ist ein Messproblem, kein Rechenproblem: die drei sichtbarsten Defekte (der Anker-Median-Zickzack jeder Laufform-Zeile, der um 25 % zu dünne Strich, der 13,6°-Knick an jedem Verbinder-Austritt) sind für JEDES eingefrorene Lineal unsichtbar, und die dafür vorregistrierte humanbench-Wortrunde mit der Echtheitsfrage wurde nie gefahren; solange sie fehlt, ist jede Verbesserung am Duktus unbeweisbar und jede Adoption Geschmackssache. Verschärfend kommt hinzu, dass die Messgrundlage selbst nicht mehr identifizierbar ist: die Wurzel hinter den aug30-Zahlen ist nie als Re-Baseline deklariert worden, der Wordbench druckt seine Fixture-Wurzel nirgends, und `tools/pairlab`s „exakte Mathematik des Produktions-Connectors“ ist seit dem 11. Juli eingefroren, während der echte Join-Block dreimal umgebaut wurde — in der Girlanden-Klasse liegen beide Kurven 0,12 xh auseinander. Der dritte Hebel ist die Vorzeige-Fläche für Fremde: kein einziger Sicherheits-Header und kein `Cache-Control` auf der SPA-Hülle (anyplot hat beides seit seinem Audit), 26 Advisories in vier Laufzeitpaketen des ausgelieferten Images, alle fünf GitHub-Sicherheitsschalter aus (im Schwesterrepo alle an), ein Codecov-Badge, der 87 % zeigt, wo ehrlich ~59 % stehen, 121 Altlast-Branches gemergter PRs neben dem README, und ein Bot-Serving-Wächter, der seit drei Nächten rot ist, ohne dass es jemand erfahren kann. Dazu ein Fund, der nirgends sonst auftauchen konnte: in der öffentlichen Git-Historie liegt seit dem 20. Juni ein 32-KB-Blob mit den Diagnose-Payloads zweier reservierter Templates — ausgerechnet von dem PR entfernt, der „den Moat härtet“, aber nie aus der Historie, und das Netz des Lizenz-Skills kann so etwas gar nicht finden. Ehrlich dazugesagt: nichts davon ist Architektur. Es gibt keinen Modul-Split, keinen Umbau, keine Neuerfindung in dieser Liste — die Grenzen liegen in einem Schätzer, einem Render-Parameter, einer fehlenden Klassenregel, drei Tafelformen, ein paar Schaltern und in Texten, die mehr versprechen als die Sache hält.

## Erledigungsstand (2026-09-03, aktualisiert 2026-09-05)

Nachgetragen am 2026-09-03 und am 2026-09-05 fortgeschrieben — der einzige nachgetragene Abschnitt bleibt dieser: der Bericht selbst bleibt, was er ist — ein Befundprotokoll vom 2026-09-02, dessen Titel, Nummern und Zeilen nicht fortgeschrieben werden. Hier steht nur, was seither daraus geworden ist, damit ein Leser den Stand nicht aus 39 Befunden, 14 Tracks und PRs bis #542 zusammensuchen muss. Alle genannten PR-Nummern sind gegen `gh pr view` geprüft; alle sind gemergt (auch #507, das am 2026-09-03 noch als Entwurf offen lag). Von den 39 Befunden sind 33 erledigt, 5 teilweise erledigt, und einer (Befund 19, Austritts-Kollinearität) gemessen und ehrlich verworfen — kein Befund ist mehr unangetastet, keiner wartet mehr auf einen bloßen Autor-Entscheid ohne Anschlussarbeit.

### Die 39 Befunde

| Nr | Status | Beleg |
|---|---|---|
| 1 | erledigt | Lesart-Ehrlichkeit in #476; das Wörterbuch ist am 2026-09-02 direkt geladen worden, der Ladeweg dafür steht in #477 |
| 2 | erledigt | #476 — das Anzeigebild und der PDF-Bauer teilen wieder denselben Cache-Eintrag |
| 3 | erledigt | #499; nach dem Entscheid zu F4 bleibt die Lineatur unantastbar, die Seite weist stattdessen sichtbar hin |
| 4 | erledigt | #486 misst den glatten Zeilen-Schätzer trocken, #501 adoptiert ihn als Autor-Entscheid (2026-09-03): die humanbench-Runde steht 40:1 für den Kandidaten, verfehlt die vorregistrierte Tie-Schranke aber knapp; keine Wiederholung, und der PR hält fest, dass die Adoption der Entscheid des Autors ist und nicht das Verdikt des Instruments |
| 5 | erledigt | #484 schließt das Netz (Inhalts-Pickaxe im Lizenz-Skill), #500 nimmt den Blob dokumentiert an (F9) und führt die 13 Blobs namentlich per Hash |
| 6 | teilweise | #497 liefert alle Header aus; die CSP läuft jetzt auf dem Nonce-Pfad statt der zwei Hashes (#532), weiterhin `Report-Only`, scharf frühestens 2026-09-10 |
| 7 | erledigt | #481 — 29 Advisories auf 0, plus der Audit-Job in der CI |
| 8 | erledigt | am 2026-09-02 per API gesetzt: die fünf Sicherheitsschalter, Squash-only, Branch-Löschen beim Merge und Pflicht-Checks über alle sechs CI-Jobs |
| 9 | erledigt | #478 gibt dem Wordbench Wurzel-Digest und `--expect-root`, die Wurzel ist am 2026-09-02 aufgefrischt, #489 trägt den Headline-Ledger nach |
| 10 | erledigt | #480 baut den Wortmodus mit der Echtheitsfrage, die erste Runde ist am 2026-09-02 gefahren worden |
| 11 | erledigt | #479 — Erwartungen aus `app/prerender/` abgeleitet, Alarmpfad über ein Issue |
| 12 | erledigt | #479 macht die Frontend-Messung ehrlich, #498 stellt zwei Flag-Badges nebeneinander (F8) |
| 13 | erledigt | #487 bringt Dirty-Guard, Sperrzustand und die deutsche Fehlerschicht; #513 entscheidet A12 (2026-09-03): die Sperre bleibt offen und sichtbar markiert, überschreiben braucht ein bewusstes „Trotzdem überschreiben“ (`force`), keine Entsperrung mehr vorweg |
| 14 | erledigt | #490 (Limiter über alle Routen), #493 (Origin-Gate gegen die direkte `run.app`-Tür), dazu die Cloudflare-Regeln — Rate-Limit und Transform-Rule — und der Worker als Quelle im Repo in #495 |
| 15 | erledigt | #484 |
| 16 | erledigt | #475 — beide Audit-Dokumente sind seither getrackt |
| 17 | erledigt | #489 gibt §14 sein Register und ein CI-Gate dazu; #521 verschiebt §14 wortgleich nach `messjournal.md` (löst A17 — als einzige Sektion der Datei entfällt „vor §15 verschieben“ als Frage); A16 bleibt bewusst als `undeklariert` dokumentiert (Nachtrag `sep02` aus #489, jetzt in `messjournal.md`) — die Herkunft ist nicht rekonstruierbar, Zahlen ab `aug30` gelten nur untereinander vergleichbar |
| 18 | erledigt | #527 lässt `pairlab` den Produktions-Connector per Recorder-Replay erneut messen statt ihn nachzubauen: 89 von 248 Joins bewegt (`gen_chamfer` 0,0434→0,0392), der eingefrorene Spiegel bleibt mit ehrlichem Docstring bestehen |
| 19 | gemessen & verworfen | #488 — der Austritts-Trim (Arm J4) ist gegen die Hand gemessen und als ehrliches Negativ verworfen, die Rettungswege stehen in §7.9 |
| 20 | teilweise | F5 ist mit „ja“ beantwortet; #526 registriert die Platten-Nib-Wortrunde vor (A3) und baut sie dry — die Nebenbedingung ist schon gerissen (`gleichzug_doublings` 13→21), ein Autor-Urteil steht noch aus |
| 21 | erledigt | #485 (Fokus, Kontrast, Typo-Boden, Trefferflächen), #504 macht die 44-px-Regel in `design-system.md` §9 bindend, #505 zieht den vollen Sweep über 255 Ziele nach |
| 22 | erledigt | #483 |
| 23 | erledigt | #479 macht den Bot-Wächter driftfest; Trigger-Pfadliste geprüft 2026-09-04 — `app/prerender/**` steht in den `deploy-api`-Includes, kein PR gefunden oder nötig, der sie geändert hätte |
| 24 | erledigt | Ruleset gesetzt und die Bot-Zweige am 2026-09-02 per `@dependabot rebase` entblockt; #466, #467 und #496 sind gemergt |
| 25 | erledigt | `delete_branch_on_merge` ist an, und der Altbestand ist weg: `git ls-remote --heads origin | wc -l` = 1 (2026-09-05) |
| 26 | erledigt | #481 — der 4-Dezimal-Vertrag gilt jetzt auch für die Antwort |
| 27 | erledigt | #497 — Bytecode im Builder und im Runtime-Stage |
| 28 | erledigt | #485 hebt Trefferfläche und Typo-Boden; #535 baut alle drei Stufen der Federprobe (↺ auf eigenem Grund, Breite aus der echten Messung, Umbruch ab 14 px x-Höhe — Autor-Entscheid 2026-09-04); #537 erweitert dieselbe Fläche zur Postkarte (480 Zeichen, Größenstufen, Absätze) |
| 29 | erledigt | #476 verlinkt die Quiz-Auswertung auf die Tafel; #539 füllt die Bank-Lücke (42→77 Wörter, 64 von 66 Glyphen mit modernem Bankwort, c/q bewusst als Clusterwort) |
| 30 | erledigt | #476 gibt den drei Tafelscans ihre Maße aus `chart_size`; #522 misst die CLS-Ursache in Produktion nach — nicht die GLKurrent-Initiale (schon geladen, bevor der Sprung kommt), sondern der späte Aufbau der drei Abschnitte — und schließt sie mit `TafelSkeleton` (mobil 0,0969→0,0007, Desktop 0,1125→0,0004); der font-display-Punkt ist damit hinfällig, der Autor verzichtet auf die Änderung (Veto) |
| 31 | erledigt | #479 — README-Schlüsseltupel korrigiert, Community-Health-Dateien ergänzt |
| 32 | erledigt | #479 plus das Ruleset: Image-Job, Migrations als Pflicht-Check, App-Deploy über die Kandidatenkette |
| 33 | teilweise | F6/A4 entschieden (2026-09-03); #530 misst beide Hälften: die Anstrich-Klassenregel für t/ſ/k/ß ist gemessen & verworfen (Nebenbedingungen rot, Rettungsweg humanbench Runde 6/J5 gebaut, nicht gefahren), der d-Auslauf-Generator (b) misst grün und wird empfohlen, bleibt aber aus — Autor-Apply + Golden-Rebake offen (Todoist `6hQjvmw8H6r7H9xJ`); d-Schleifenbreite und r-Fahne bleiben Wizard-Aufgabe |
| 34 | teilweise | #484 bringt die Copilot-only-Regeln und die Gedächtnis-Direktiven in beide Anleitungen und pinnt sie per Test, #506 lagert die Begründungen aus; die Variante zu F11 (Import-Dedup statt Hand-Sync, A8) ist offen |
| 35 | teilweise | das Loch im Korb-Protokoll ist mit #487 geschlossen; #529 baut das dritte Inventar-Flag „unter dem Boden“ und misst LF12 dry — heute VIER Zeilen betroffen statt zwei (`P` n=1, `S` n=2 ohne Fits, `p` 3→2, `s` n=1), der Write bleibt Autorensache |
| 36 | erledigt | #507 gemergt — Rechtsgrundlage, Widerspruchs- und Beschwerderecht ergänzt, Cloudflare-Standortaussage abgeschwächt |
| 37 | erledigt | #489 und #484 räumen `werkzeuge.md` und die Skill-Lücke; #510 macht `dev_ids()` reproduzierbar (`tuple(sorted(...))`, lauter Fehler statt stillem `except ImportError`) |
| 38 | erledigt | #484 (Provenienz, igerman98 im Index) und #502 (Geltungsbereich in LICENSE) |
| 39 | erledigt | #481 gibt `apiFetch` ein `AbortSignal.timeout` (Finding 39a); #515 räumt den Lint-Parkplatz aus #227 (69→0) und riegelt ihn mit `--max-warnings 0` |

### Die 14 Tracks

| Track | Gegenstand | PRs |
|---|---|---|
| T1 | Laufform LF11 „Glatte Zeile“ | #486 (trockene Messung), #501 (Adoption) |
| T2 | Wordbench-Sensorik: Naht-Winkel und Wurzel-Identität | #478 |
| T3 | Austritts-Kollinearität | #488 (gemessen und verworfen) |
| T4 | humanbench-Wortmodus mit der Echtheitsfrage | #480, #492 |
| T5 | Journal-, Verfahrens- und Kampagnen-Register | #489 |
| T6 | Die zwei toten Werkzeuge | #476, dazu #477 für den Ladeweg des Wörterbuchs |
| T7 | Übungsblatt-Robustheit | #499 |
| T8 | Öffentliche Bedienbarkeit | #485, #504, #505 |
| T9 | Auslieferung und Image härten | #497 |
| T10 | Inhalt und SEO-Ehrlichkeit | #483, #503 |
| T11 | Werkbank-Politur | #487 |
| T12 | Repo-Vorzeige und CI-Wächter | #479, #498 |
| T13 | API-Hygiene, Zulieferkette und Client-Robustheit | #481, #490, #493, #495 |
| T14 | Skills, Agent-Anleitungen und Daten-Provenienz | #484, #500, #502, #506 |

### Die elf Fragen an den Autor

| Frage | Antwort |
|---|---|
| F1 | Ja — das Wörterbuch ist am 2026-09-02 direkt geladen worden (Ladeweg #477) |
| F2 | Ja, gesetzt — die acht Repo-Einstellungen sind am 2026-09-02 per API umgelegt |
| F3 | Ja — CSP erst eine Woche `Report-Only`, HSTS 180 Tage ohne Preload wie in anyplot (#497) |
| F4 | Hinweis — die Lineatur bleibt unantastbar, die Seite sagt sichtbar, was sie weglässt (#499) |
| F5 | Ja — der Platten-Nib, aber erst nach der humanbench-Runde, die auf LF11 folgt |
| F6 | Entschieden (A4, 2026-09-03) — Anstrich-Klassenregel t/ſ/k/ß gemessen & verworfen, d-Auslauf-Generator (b) empfohlen und ausstehend (#530) |
| F7 | Erledigt — die Runde ist am 2026-09-02 gefahren worden (Instrument: #480, #492) |
| F8 | Zwei Flag-Badges statt einer verrechneten Zahl (#498) |
| F9 | Annehmen — der Blob bleibt in der Historie, dokumentiert und gegen den nächsten Fall abgesichert (#500) |
| F10 | Ja, nach LF11 — mit dessen Adoption ist der Prod-Re-Harvest jetzt fällig |
| F11 | Hand-Sync plus Pin-Test (#484, #506); das Import-Dedup bleibt offen |

### Schwesterrepo anyplot

Die Schwesterregel — was in einem Repo gut gelöst ist, geht sofort ins andere — ist in derselben Bewegung eingelöst worden: sechs PRs in `anyplot`, alle am 2026-09-02/03 gemergt. #11205 baut das API-Image in der CI und fährt vor dem Merge einen Container-Smoke, #11207 schickt den Frontend-Deploy über eine Kandidaten-Revision mit eigenem Smoke, #11209 gibt dem Bot-Wächter den Alarmpfad und leitet seine Erwartungen ab statt sie einzubetonieren, #11206 schließt PYSEC-2026-2132 und pinnt die Node-Version des Builds, #11210 pinnt die Agenten-Anleitungen per Test und repariert die Drift, die der Pin findet, und #11208 schließt die direkte `run.app`-Tür mit demselben geteilten Origin-Geheimnis — dort seit 2026-09-03 scharf. Die Cloudflare-Rate-Limiting-Regel steht in anyplot ebenfalls. Was in diese Richtung noch offen ist — Bytecode im API-Image und die CSP-Härtung —, liegt als eigene Aufgabe vor.

### Was offen bleibt

Von den sechs Entscheidungen der Todoist-Aufgabe „Abend-Entscheidungen 2026-09-02: Audit-Tracks“ (`6hQFPQJ8j9f6J25r`) sind vier gefallen: A12 (Sperr-Doktrin im Wizard, Befund 13, #513), A4 (Tafelformen und d-Auslauf, Befund 33 / F6, #530 — Anstrich-Klassenregel verworfen, d-Auslauf-Generator empfohlen und ausstehend), A16 (Herkunft der aug30-Wurzel, Befund 17 — dokumentiert als `undeklariert`) und A17 (die drei jüngsten §14-Abschnitte vor §15 verschieben, Befund 17 — durch die Auslagerung nach `messjournal.md` in #521 gegenstandslos). Offen bleiben A3 (Platten-Nib nach der humanbench-Runde, Befund 20 — Wortrunde vorregistriert und dry gebaut in #526, Autor-Urteil aus) und A8 (Import-Dedup statt Hand-Sync, Befund 34 / F11, die Aufgabe „Modellentscheidung d-Auslauf“ `6hFvQgG3H88R6vmr` ist mit A4 erledigt). Der Datenschutz-Wortlaut zu Befund 36 ist mit #507 gemergt. Die CSP führt weiterhin zwei eigene Aufgaben: die Werkbank in der Report-Only-Woche mit offener Konsole durchgehen (`6hQMgxPG8pFvF4gr`) und danach scharf schalten, mit vorher geprüftem Melde-Weg (`6hQMgxQGXm23877r`, frühestens 2026-09-10, unverändert). Dazu kommen die drei Entscheidungen zur `/write`-Absicherung (`6hQFFx9VrpjGJMJr`, Befund 14), der Blick auf die Werkbank-Dichte nach dem angehobenen Typo-Boden (`6hQG36Jf6GH2V5VJ`, Befund 21) und der Rücktransfer nach anyplot (`6hQMgxQfGqXcCr4J`).

Von den sechs Befunden, die am 2026-09-02 ohne PR und ohne Aufgabe standen, sind fünf erledigt: der eingefrorene pairlab-Spiegel (18, #527 — Recorder-Replay statt Nachbau), die Pfadliste des `deploy-api`-Triggers (23, geprüft 2026-09-04: `app/prerender/**` steht drin), der Altbestand der 121 Remote-Zweige (25, gelöscht — `git ls-remote --heads origin` zählt 1), die Reproduzierbarkeit von `dev_ids()` (37, #510) und der Lint-Parkplatz aus #227 (39, #515). Offen bleiben allein die Laufform-Zeilen unter dem Beweis-Boden (35) — LF12 (#529) zeigt inzwischen vier statt zwei (`P`, `S`, `p`, `s`); der Write bleibt Handarbeit des Autors.

### Nachzug 2026-09-04/05: was aus der A-Liste dazukam

Die Audit-Arbeit selbst hat neue A-Punkte aufgeworfen, die keiner der 39 Befundzeilen zugeordnet sind, aber aus derselben Runde stammen und hier mitgeführt werden, statt in einer eigenen Momentaufnahme zu verschwinden. Je eine Zeile, PR-Nummer als Beleg:

- **A28** — g/p als Lesefalle im Quiz plus der Verwechsler-Fold im Lesart-Wörterbuch → #534 (versionierter Lesart-Schlüssel, `LESART_KEY_VERSION` v2, Generation 2 der Ladung).
- **A30** — der Site-Container (`kurrentschrift-app`) bekommt ein Origin-Gate wie zuvor die API → #536, Übertrag aus dem Schwesterrepo (anyplot #11221, dort am 2026-09-04 scharf); hier ausdrücklich mit `ORIGIN_GATE=off` ausgeliefert, das Scharfschalten ist ein eigener, noch offener Rollout-Schritt.
- **A29b** — CSP-Hashes durch ein Nonce ersetzt, weil Cloudflares JavaScript Detections kein Hash erfassen kann → #532, Übertrag aus anyplot #11220; Policy bleibt `Report-Only`.
- **A39** — die „Vorlage neu abgeleitet“-Bestätigung im Diagnose-Dialog war nie sichtbar: ein Render-Phasen-Guard setzte das Flag in derselben Render-Passage zurück, in der es gesetzt wurde → #533.
- Folgearbeit aus #533: die Label-Spalte des Score-Breakdowns lief in den eigenen Balken, und der Dialog scrollte bei 390 px seitwärts → #541.
- Die Spur-Werkzeuge bekommen dieselbe Wurzel-Absicherung wie der Wordbench (`--expect-root`, Digest vor der ersten Messung) → #540.
- Die Image-Importmessung aus `docs/notes/serve-image-importgraph-2026-09-04.md` (#523) wurde im image-geformten venv wiederholt und bestätigt: kein Code geändert → #542.
- Retro der Session 2026-09-04/05 (`/optimize-skills`): die Edit/Write-Regel bekommt ihre Rangfolge gegenüber einer Harness-Erinnerung, `/open-pr` sein neues „ready“-Kriterium nach der Ruleset-Änderung → #538.

## Stärken

- Das Auftragskorb-Protokoll ist nicht nur Doktrin, sondern API-erzwungen UND wird eingehalten: alle fünf Live-Zeilen tragen `understanding`, `reproduced`, `stage` und `resolution`, jede `done`-Zeile nennt ihre PR, und alle fünf zugehörigen PR-Bodies nennen umgekehrt „Korb #N“ — die Quer-Verweis-Pflicht aus §5.3 ist zu 100 % erfüllt.
- Die Pre-Reg-Disziplin hält unter Prüfung: bei allen fünf Stichproben mit neuem Arm (LF5/LF6/J1, LF7/LF8, J2/J3/LF9, LF3b-W, Kette v5) steht in den Squash-Bodies ein Commit „pre-register … before the first number“ VOR dem Mess-Commit; alle 74 §14-Einträge tragen Gates, Verdikt und Rettungswege, die Verworfen-Listen sind geschlossen.
- Die API-Deploy-Kette ist das Vorzeigestück: Migrate-Job vor dem Rollout, `--no-traffic` + `--tag=candidate` + deterministischer Revision-Suffix, Smoke gegen genau diese Revision (Health, /styles, /write/word, 401-Gate), erst danach `update-traffic` — 40 von 40 Cloud Builds SUCCESS.
- Der Open-Core-Schnitt ist gepinnt statt behauptet: `tests/test_api_public_surface.py` klassifiziert jede GET-Route, `require_admin` vergleicht timing-sicher mit `secrets.compare_digest`, verifiziert das Cloudflare-Access-JWT mit Algorithmus/Aussteller/Audience, fällt ohne Konfiguration mit 503 geschlossen aus und loggt nie einen Header-Wert.
- Doku-Hygiene über 52 Dokumente / 28.339 Zeilen: 467 relative Links inklusive Anker lösen auf — null tot; jedes Doc außer den bewusst kopf-freien Faktenblättern trägt seinen Status-Blockquote, kein Status-Datum ist älter als 30 Tage.
- Die Rechtelage im HEAD ist auffallend gepflegt: sieben Quellen mit vollständigem SOURCE.md, alle 12 vermerkten SHA256 stimmen mit den Bytes, `sha256sum -c` über die 22 Leitfaden-Seiten grün, der Süß-Test leer, kein Raster in einem SVG, die gitignore-Grenzen zu 100 % dicht (22 Eigenhand-Dateien, 6 Korpus-Dateien, 1291 Bench-Fixtures).
- Die Skills sind ausführbare Anleitungen mit Erwartungswerten, Gotchas aus echten Läufen und Troubleshooting — in Sprache und Doktrin (Änderungspfad-Regel, „Verworfen ist entschieden“, Korb-Protokoll, Glossar-Gate) deutlich reifer als die anyplot-Zwillinge.
- Die Frontend-Laufzeit ist sauber: WAAPI statt globaler Keyframes, jede Animation mit `cancel()`, jeder Timer geräumt, Wort-Cache FIFO-gekappt, Federprobe entprellt — kein Leak gefunden; die Schreibanimation läuft live mit 60 fps (Median 16,7 ms, 0 Frames > 32 ms über 3 s) und übersteht einen Resize ohne Neustart.
- Die deutschen Texte sind in ungewöhnlich gutem Zustand: 793 sichtbare Strings ohne einen Tippfehler, Anführungszeichen/Apostroph/Gedankenstrich durchgehend korrekt, kein hartkodierter Prosa-String in den öffentlichen Views, der Briefton um 1900 trägt vom Impressum bis zur Lesart-Seite — und die Quiz-Rückmeldung nach einem Fehlgriff (beide Formen nebeneinander plus Merkmalssatz) ist das Beste an der ganzen Seite.
- Die Sprachregelung wird zu 100 % eingehalten: über core/, api/, tests/, alembic/ null deutsche Kommentare oder Docstrings; die fünf Treffer in tools/ und app/src sind wörtliche Zitate innerhalb englischer Prosa. Ebenso die Werkbank-Doktrin: live NULL `glyph_pairs`-Overrides, `core/compose.py` mit 14 benannten Klassenmengen statt Einzelfall-Patches, und der Markieren-Dialog stellt die Vorsortier-Frage tatsächlich, bevor ein Auftrag entsteht.

## Rangliste der Befunde

### 1. [hoch] /lesen/vergleichen ist in Produktion tot — und behauptet trotzdem, die Lesart sei „wohl eindeutig“

- **Bereiche:** live-desktop, live-mobile, api · **Aufwand:** S · **KI jetzt** · **Owner:** Der Ladelauf selbst: `tools.lesarten.sync` schreibt ≈720.000 Formen über die admin-gesicherte API in die GETEILTE Cloud-SQL-DB — prod-berührend, braucht ADMIN_TOKEN und die ausdrückliche Freigabe in derselben Sitzung.
- **Warum:** Von mir live nachgeprüft: `curl https://api.kurrentschrift.ink/lesarten?text=Muhme` → `{"readings":[],"dictionary":null}`, `/lesarten/dictionary` → `null`. Die Seite zeigt daraufhin ZWEI sich widersprechende Sätze untereinander: kursiv „Kein Wort im Wörterbuch sieht dieser Lesart zum Verwechseln ähnlich — sie ist wohl eindeutig.“ und darunter klein „Das Wörterbuch ist noch nicht geladen“. Betroffen sind auch die vier eigenen Beispiel-Chips (Muhme · Wittib · Taufe · Häusler). Das Kernwerkzeug für Genealogen ist tot und lügt dabei — schlimmer als ein sichtbarer Ausfall, weil es eine falsche Schlussfolgerung ausspricht. llms.txt und der Lesen-Hub versprechen es zusätzlich Maschinen und Menschen.
- **Vorschlag:** (a) Sofort ohne Autor: `app/src/sections/vergleichen/VergleichenView.tsx:235` nach Wörterbuch-Zustand aufteilen — `readings.length === 0 ? (dictionary ? t.noLesarten : t.dictionaryMissing) : …`; die separate Wörterbuch-Zeile (:272-274) nur noch rendern, wenn `dictionary` existiert; neuer Locale-Key in `app/src/locales/de/vergleichen.ts` im Briefton; Vitest über die drei Zustände (kein Wörterbuch / Wörterbuch + 0 Treffer / Wörterbuch + n Treffer), der `dictionary: null` gegen den „eindeutig“-Satz pinnt. (b) Danach der Autor: `ADMIN_TOKEN=… uv run python -m tools.lesarten.sync` gegen api.kurrentschrift.ink. Bis dahin die Zusage auf dem Lesen-Hub (`locales/de/hub.ts`) und in `app/public/llms.txt` entschärfen.

### 2. [hoch] „Lesetafel als PDF“ ist live kaputt — CORS-Cache-Kollision zwischen Anzeigebild und PDF-Bauer

- **Bereiche:** live-mobile, app · **Aufwand:** S · **KI jetzt**
- **Warum:** Frisches Laden mit ignoreCache auf /tafel, Klick auf „Lesetafel als PDF“: nach 45 s kein Blob, stattdessen „Das PDF konnte gerade nicht erstellt werden.“ Konsole: CORS-Block auf `…/sources/loth-1866/chart`. Der Server ist in Ordnung (mit Origin-Header 200 + ACAO). Ursache: `app/src/sections/tafel/TafelView.tsx:191-200` lädt die Tafel als `<img>` OHNE `crossOrigin`, `useLesetafelPdf.ts:26-32` setzt danach `img.crossOrigin='anonymous'` auf dieselbe URL — Chrome reicht den ohne CORS-Modus gecachten Eintrag durch, ACAO fehlt, blockiert. Genau das ausdruckbare Blatt zum Danebenlegen ist das, was ein Genealoge sucht.
- **Vorschlag:** `crossOrigin="anonymous"` an das Anzeige-`<img>` in TafelView.tsx:193 setzen, damit Anzeige und PDF-Bauer denselben Cache-Eintrag im CORS-Modus teilen; Kommentarkopf in `useLesetafelPdf.ts` um den Grund ergänzen, warum das Attribut drüben stehen MUSS; Vitest, der `crossorigin === 'anonymous'` pinnt. Live-Gegenprobe: Seite frisch laden, OHNE zu scrollen tippen, Blob-Größe prüfen; einmal mit vorher gescrollter Seite. Zweite Absicherung im api/-Scope: die Chart-Route ACAO unbedingt setzen lassen statt nur Origin-abhängig.

### 3. [hoch] Übungsblatt: eine Vorschrift-Zeile ab ~18 Zeichen verschwindet still — der eigene Hilfetext verspricht 60

- **Bereiche:** live-desktop · **Aufwand:** M · **braucht Owner** · **Owner:** Doktrin-Entscheid: darf die Vorschrift verkleinert/umgebrochen werden (dann stimmt ihre Höhe nicht mehr exakt zur Lineatur), oder bleibt die Lineatur unantastbar und die Seite weist nur hin? Das entscheidet, ob (1) oder nur (2)+(3) gebaut wird.
- **Warum:** Gemessen an den Glyph-Pfaden des Vorschau-SVG: 10 Zeichen → 20 Pfade, 15 → 30, ab 20 → 0. Ein gewöhnlicher Satz („heute schreibe ich Dir aus Straßburg.“, 37 Zeichen) erzeugt ein komplett leeres A4-Blatt ohne jeden Hinweis; der einzige Hinweis steckt im geschlossenen „Mehr dazu“-Popover und nennt eine andere Zahl. Zweite Ausprägung: bei zwei Zeilen fällt die zweite kommentarlos weg. Der Schreiben-Hub verspricht ausdrücklich „auf Wunsch mit deinem Text als Vorschrift in den Zeilen“, und der PDF-Knopf bleibt aktiv.
- **Vorschlag:** Strukturell statt kosmetisch: (1) eine zu breite Zeile in `useWorksheetText.ts` an einer Wortgrenze umbrechen oder mit `min(1, verfügbareBreite/Wortbreite)` skalieren; (2) sichtbare Rückmeldung DIREKT unter dem Textfeld mit Zeilennummer statt im Popover; (3) den Hilfetext in `app/src/locales/de/worksheet.ts:18` ehrlich machen — die tatsächliche Zeichenzahl aus Mittellänge und Seitenrand berechnen; (4) den PDF-Knopf sperren oder warnen, wenn die Vorschau keine Vorschrift enthält, obwohl Text eingegeben ist. Regressionstest: 37-Zeichen-Zeile bei 6 mm liefert Skalierungsfaktor < 1 oder Warn-Eintrag, nie ein leeres Ergebnis.

### 4. [hoch] Laufform-Zeilen zittern: 2–11 Zickzacks pro x-Höhe, wo die Tafelzeile 0 hat — sichtbar in jedem gebundenen Wort

- **Bereiche:** geschriebenes, core · **Aufwand:** M · **KI jetzt** · **Owner:** Nur der Write der neuen Zeilen in Prod (Autor-Go + dbsnapshot) und das Echtheits-Urteil in der Paarrunde; die Messung und die Kandidaten-Karte sind trocken.
- **Warum:** Auf 0,02 xh resampelte Centerlines, Krümmungs-Vorzeichenwechsel > 3°/xh: n 0,46 → 2,81 · o 0,00 → 11,25 · i 0,89 → 10,34 · e 0,00 → 2,74. Ursache: `core/aggregate.py:61-73 _median_and_mad` medianisiert jeden der 120 Anker unabhängig; gerendert wird die Zeile in jedem Lauf ≥ 3 (`core/compose.py:1662-1667`), also in praktisch jedem öffentlichen Wort. Kein Lineal sieht es — Wordbench und Tracebench resampeln den Zickzack weg. Das ist der größte einzelne Unterschied zwischen „geschrieben“ und „gerechnet“ im ganzen Produkt und damit der Kern des Owner-Ziels.
- **Vorschlag:** Trockenes Experiment „LF11 Glatte Zeile“ (Arm-Nummer LF10 ist durch PR #474 belegt — Namenskollision im selben PR klären): die Zeile nicht als Per-Anker-Median bilden, sondern je Vorkommen den Fit pro Zug auf eine B-Spline mit Knoten alle ~0,08 xh projizieren (Ecken/Strichanfänge aus `trace_meta.corner_anchors`/`stroke_starts` als feste Knoten), Median über die Kontrollpunkte, zurück auf 120 Anker. Gebaut in `core/aggregate.py` + eigenem Builder neben `tools/laufform/harvest.py`, Fixture-Wurzel eingefroren. Pre-Reg-Gates wie LF3b-W: word_loss/pair_loss ≤ +0,002 · Glätte-Sensor schließt ≥ 50 % der Lücke zur Tafel · LF8-Sprung- und LF9-Kopf-Gate je Zeile grün · Kompositions-Soll ohne Verlust · Lotse/Kette dev-19 unverändert. Kill: ein Gate rot; Rettungsweg dann Glättung nur der n ≥ 5-Zeilen.

### 5. [hoch] Reservierter Datensatz liegt in der ÖFFENTLICHEN Git-Historie — und das Audit-Netz kann ihn nicht finden

- **Bereiche:** daten-lizenz, repo-hygiene · **Aufwand:** M · **braucht Owner** · **Owner:** Entscheidung Purge (History-Rewrite auf einem öffentlichen main) vs. dokumentiertes Annehmen. Schritt (1) — Skill-Netz plus Doku-Zeile — braucht diese Entscheidung nicht.
- **Warum:** `.design-sync/previews/_writtenGlyphData.ts` (32.219 Bytes) wurde am 2026-06-20 (Commit 84c6332, PR #108) hinzugefügt und am 2026-07-31 von PR #254 („Harden the open-core moat“) nur aus HEAD entfernt, nie aus der Historie. Der Blob trägt die Diagnose-Payloads zweier Templates (`skeleton_polyline_px`, `half_widths_px`, `anchors`, `outline`) — genau die Route, die heute als RESERVIERT gepinnt ist. Das Repo ist seit 2026-05-19 öffentlich. Warum es nie auffiel: `.claude/skills/audit-licenses/SKILL.md:56` filtert die History-Sweep auf Binär-Endungen — eine `.ts`-Datei kann dort nicht auftauchen, obwohl der Gotcha-Absatz genau diesen Fall beschreibt.
- **Vorschlag:** Zwei getrennte Schritte. (1) Netz sofort schließen (KI, ohne Owner): Inhalts-Pickaxe in den Skill — `for pat in skeleton_polyline_px pixel_anchors half_widths_px anchors_template outline_paths; do git log --all -S"$pat" --diff-filter=A --name-only …; done` mit der Regel „jeder Treffer außerhalb core/, api/, tools/, tests/, docs/ ist ein Befund“, plus eine Zeile in der „Verified baseline“, die diesen Blob namentlich als bekannten Fall führt. (2) Umgang mit dem Blob = Owner-Entscheid: ANNEHMEN (2 von ~80 Glyphen, 32 KB, überholte Geometrie → ein Satz in `quellen-und-rechte.md` §5 unter „Bekannte, akzeptierte Ausnahme“) oder PURGEN (`git filter-repo --invert-paths`, force-push auf öffentlichen main, GitHub-Support für den gecachten Blob) — mit der ehrlichen Auskunft, dass jeder bestehende Klon und Fork die Kopie behält.

### 6. [hoch] kurrentschrift.ink liefert keinen einzigen Sicherheits-Header und kein Cache-Control auf index.html — anyplot hat beides

- **Bereiche:** live-desktop, security, perf-kosten · **Aufwand:** M · **KI jetzt** · **Owner:** Zwei Weichen: (a) CSP eine Woche als Report-Only oder direkt scharf — ein Fehler dort macht die Werkbank unbenutzbar; (b) HSTS 180 Tage ohne includeSubDomains/preload wie anyplot bestätigen.
- **Warum:** Von mir live nachgeprüft: `curl -sSI https://kurrentschrift.ink/` liefert HTTP/2 200 ohne `strict-transport-security`, ohne `content-security-policy`, ohne `x-content-type-options`, ohne `x-frame-options`, ohne `referrer-policy`, ohne `cache-control`. anyplot.ai trägt alle sechs aus `app/security-headers.conf` (Kopfkommentar: „audit 2026-07-16“). Praktische Folge des fehlenden Cache-Control: die Antwort trägt nur `last-modified`, der Browser cachet die SPA-Hülle heuristisch mit ~10 % des Alters — nach einem Deploy zeigt sie auf `/assets/`-Hashes, die `try_files $uri =404` beantwortet: weiße Seite. Die App läuft dabei mit maxScale 1 ohne minScale, jeder Seitenaufruf geht also an genau diesen Origin.
- **Vorschlag:** `app/security-headers.conf` aus anyplot übernehmen und die CSP auf die tatsächlichen Quellen dieser Seite schreiben: `connect-src 'self' https://api.kurrentschrift.ink https://plausible.io`, `img-src 'self' data: blob: https://api.kurrentschrift.ink`, `font-src 'self' data:` (Fonts selbst gehostet), `script-src 'self' 'unsafe-inline'` (Plausible kommt über den eigenen `/js/script.js`-Proxy), `object-src 'none'; base-uri 'self'; frame-ancestors 'self'`. In `app/nginx.conf` per `include` im Server-Block UND in jedem `location`-Block mit eigenem `add_header` ziehen (nginx merged nicht über Ebenen — genau der Fallstrick, den anyplots Kopfkommentar festhält); `location = /index.html { add_header Cache-Control "no-cache"; }` VOR `location /` (bewusst `no-cache` statt anyplots `no-store`, damit der gemessene 304-mit-0-Byte-Pfad erhalten bleibt — Grund als Kommentar, sonst „repariert“ ihn der nächste Abgleich zurück); im Dockerfile mitkopieren. Für den API-Host separat eine schlanke Starlette-Middleware mit `nosniff` + `Referrer-Policy`.

### 7. [hoch] 26 Advisories in vier Laufzeitpaketen des ausgelieferten API-Images — starlette 1.0.0 sitzt im Request-Pfad

- **Bereiche:** security, ci-deploy · **Aufwand:** M · **KI jetzt**
- **Warum:** `uv export --no-dev` (exakt der Satz, den `uv sync --frozen` im Image installiert) durch `pip-audit`: starlette 1.0.0 → 5 Advisories (Host-Header ohne Validierung vor `request.url`, `request.form()` ohne `max_fields`/`max_part_size`, HTTPEndpoint-Methodenauflösung …) → Fix 1.3.1; cryptography 48.0.0 → 4 (u. a. exponentieller Blowup bei Zertifikatsketten) → 50.0.0; aiohttp 3.13.5 → 14 (u. a. SNI-Bypass) → 3.14.3; pyasn1 0.6.3 → 3 → 0.6.4. cryptography ist der Pfad, der das Access-JWT prüft. Die konkrete Ausnutzbarkeit ist hier begrenzt (kein `UploadFile`, `request.url` nur `.path`), aber ein Web-Framework fünf Advisories hinter dem Fix lässt man nicht mit Einzelfall-Argumenten stehen. CI prüft Abhängigkeiten auf gar nichts.
- **Vorschlag:** (1) `uv lock --upgrade-package starlette --upgrade-package cryptography --upgrade-package aiohttp --upgrade-package pyasn1`, `uv sync --all-extras`, volle Suite (`/verify-core` + `/verify-api`); beim starlette-Sprung 1.0 → 1.3 besonders auf die Fehlerpfade und die `GZipMiddleware`/`CORSMiddleware`-Reihenfolge schauen; `pip-audit` als Nachweis wiederholen und die Zahl ins Changelog-Fragment schreiben. Dependabot-PR #468 vorher ansehen und entweder darauf aufsetzen oder schließen, damit nicht zwei Locks konkurrieren. (2) Strukturell: Job „Dependencies (audit)“ in `.github/workflows/ci.yml` mit `uvx pip-audit -r <(uv export --format requirements-txt --no-dev)` und `npm audit --omit=dev --audit-level=high`, hart auf high/critical — zugleich Rücktransfer-Kandidat nach anyplot, das denselben blinden Fleck hat.

### 8. [hoch] Alle fünf GitHub-Sicherheitsschalter sind aus — im Schwesterrepo laufen alle fünf

- **Bereiche:** security, repo-hygiene, ci-deploy · **Aufwand:** S · **braucht Owner** · **Owner:** Alle fünf sind Repo-Einstellungen und brauchen Admin-Rechte am Repository — eine KI-Session darf sie nicht selbst umlegen.
- **Warum:** `gh api repos/…` für kurrentschrift: `dependabot_security_updates: disabled`, `secret_scanning: disabled`, `secret_scanning_push_protection: disabled`; `/vulnerability-alerts` → HTTP 404; `code-scanning/default-setup` → `not-configured`; `private-vulnerability-reporting` → false. Dieselben Abfragen gegen anyplot: alle enabled bzw. `configured` (weekly, seit 2026-07-20). Das Repo ist öffentlich, alle fünf sind kostenlos. Das ist die direkte Erklärung für den vorigen Befund: `.github/dependabot.yml` macht nur wöchentliche VERSIONS-Updates; sicherheitsgetriebene Bumps und jede Alarmierung sind abgeschaltet. Push Protection ist außerdem genau das Netz unter der Regel „Never echo secret values“ — und unter dem ADMIN_TOKEN-Unfall, der 2026-08 schon einmal Produktion gekostet hat.
- **Vorschlag:** Fünf Schalter unter Settings → Code security and analysis, in dieser Reihenfolge: Private vulnerability reporting (macht erst den in SECURITY.md und `.github/ISSUE_TEMPLATE/config.yml` bereits verlinkten Meldeweg funktionsfähig), Secret scanning + Push protection, Dependabot alerts, Dependabot security updates, CodeQL Default Setup wöchentlich (kein Workflow-File nötig, die Sprachliste steht bereit). Danach `gh api repos/…/dependabot/alerts --jq 'length'` als Startwert. Mit dem Einschalten von Secret Scanning ist ein Verlauf-Scan mit historischen Treffern zu erwarten; der Sicht-Scan auf HEAD war leer (einziger Treffer: das Testtoken in `tests/api_harness.py:25`).

### 9. [hoch] Die Messgrundlage ist nicht identifizierbar: undeklarierte Re-Baseline vom aug30, und der Wordbench nennt seine Wurzel nirgends

- **Bereiche:** journal, geschriebenes, werkbank-doktrin · **Aufwand:** M · **KI jetzt** · **Owner:** Aus welcher Sitzung / welchem Export die Wurzel hinter 0,106400 stammt — falls die Protokolle es nicht hergeben, wird sie als undeklariert markiert.
- **Warum:** §14 Z. 9013–9017 nennt für die aug29-Wurzel Wörter 0,106720 / Paare 0,146506; die Korb-Runde vom aug30 rechnet plötzlich mit 0,106400 / 0,146580, und §15 führt das als „vorher“. Kein Eintrag deklariert den Wechsel, obwohl §2 („Re-Baseline ist eine bewusste menschliche Entscheidung … datiert“) und `tintenfolger.md` §6 („ein Voll-Re-Export … ist IMMER eine deklarierte Doppel-Re-Baseline“) es verlangen. Technische Kehrseite: `tools/wordbench/run.py` druckt weder `exported_at` noch eine Prüfsumme (`grep sha256|exported_at` → 0 Treffer), die Wurzeln sind gitignored, es gibt also keinen Diff, der eine Änderung sichtbar machte — die lokalen Wurzeln tragen 2026-08-14, die dokumentierten aug29/sep01. Damit steht jede Zahl der Kampagne auf einer Basis, die niemand mehr benennen kann.
- **Vorschlag:** (1) `root_digest(root)` in `tools/wordbench/run.py` — SHA-256 über die sortierte Liste (relativer Pfad, Größe, SHA-256) aller Dateien der Wurzel — und zwei Kopfzeilen je Lauf: `root: <name> exported_at=<manifest> digest=<12 hex>`; reine Ausgabe, kein Messwert bewegt sich. (2) `--expect-root <digest>`: passt der Digest nicht, bricht der Lauf ab statt still eine andere Grundlage zu messen. (3) Das bereits vorhandene `page_sha256` aus dem Manifest im Lauf gegen die Dateien prüfen (es wird heute nur beim Neuaufbau verifiziert). (4) Nachträglicher datierter Eintrag am Ende von §14 bzw. als Vorspann zu §15: welche Wurzel, Vorher/Nachher 0,106720/0,146506 → 0,106400/0,146580, Ursache (die aug29-Löschungen), Abweichung zur Vorschau 0,107995 erklärt — ist sie nicht rekonstruierbar, als „undeklarierte Re-Baseline, Zahlen ab aug30 nur untereinander vergleichbar“ markieren. (5) Regel in §14-Kopf und `/optimize-glyphs`: jeder Eintrag mit einer Headline nennt `exported_at` + Digest seiner Wurzel.

### 10. [hoch] Die Lineale sind blind für die drei sichtbarsten Defekte — die vorregistrierte humanbench-Wortrunde wurde nie gefahren

- **Bereiche:** geschriebenes, journal, tools · **Aufwand:** M · **KI jetzt** · **Owner:** Die Urteilssitzung selbst (einziger Richter; Abkühl-Abstand zu eigenen Nachfahrungen beachten) — das Instrument kann eine Sitzung heute ohne Rückfrage bauen.
- **Warum:** Zickzack: §14 „das Lineal sieht den Zickzack nie — Resampling schluckt ihn“. Strichbreite: keine einzige Kennzahl trägt sie. Naht-Knick: unter dem 0,05–0,12-xh-Fenster des Wort-Lineals. `tintenfolger.md` §7.9 führt die humanbench-WORT-Runde als Tie-Breaker für die Methodik-Lücke UND als Rettungsweg für K-E — beide offen. `tools/humanbench/build.py` kennt nur Buchstaben-Crops, `page.py:123` fragt „Links folgt besser“ (eine Tinte-Frage), während `menschliche-bewertung.md` §8 für gleich gute Linien ausdrücklich „welche sieht echter geschrieben aus?“ verlangt. Ohne dieses Instrument ist JEDE Verbesserung am Duktus — LF11, Platten-Nib, Austritts-Trim — unbeweisbar und jede Adoption Geschmackssache. Es ist der Schlüssel zum erklärten Hauptziel.
- **Vorschlag:** humanbench-Wortmodus bauen: ein Specimen-Crop, zwei Kompositionen (Basis/Kandidat) als Overlays, die Echtheitsfrage in `tools/humanbench/page.py::CHOICES` per Schalter, Seitenzufall aus der Saat, 12 gespiegelte Wiederholungen nach Verdachtskategorie geschichtet (die §9/§10-Konstruktionslehre), Auswerteplan VORHER: Adoption bei ≥ 60 % Kandidat und ≤ 25 % „kein Unterschied“. Der Stempel je Runde nach dem Muster der beiden archivierten Runden. Erste Anwendung: LF11 (Rang 4) und Platten-Nib (Rang 20). Kosten für den Autor: ~63 Bildschirme ≈ 10 min je Kandidat.

### 11. [hoch] Der Bot-Serving-Wächter ist seit drei Nächten rot — und es gibt keinen Weg, das zu erfahren

- **Bereiche:** ci-deploy · **Aufwand:** M · **KI jetzt**
- **Warum:** `gh run list --workflow bot-serving-check.yml` → failure am 30.08., 31.08., 01.09., davor success. Ursache: PR #451 hat die Seitentitel geändert und die im Workflow einbetonierten Titel-Literale nicht nachgezogen (`git show --name-only 11bcc0e | grep -c bot-serving` → 0). Der Live-Stand ist gesund, der Wächter meldet einen Fehlalarm — und zwar seit 72 h unbemerkt, weil `permissions: contents: read` und kein Issue-, Mail- oder Slack-Schritt existiert (anyplots watchdog hat `issues: write`). Damit ist ausgerechnet das Instrument blind, das eine vierwöchige stille Crawler-Störung verhindern soll. Zusätzlich prüft er nur 4 der 11 vorgerenderten Seiten und hat gar keinen `actions/checkout`.
- **Vorschlag:** Ein PR auf `.github/workflows/bot-serving-check.yml`: (1) `actions/checkout` als ersten Schritt und die erwarteten Titel NICHT mehr literal, sondern je Datei aus `app/prerender/*.html` ziehen (Route aus dem Pfad ableiten, Titel per `grep -o '<title>…'`) — damit wird der Wächter zu dem, was er sein soll („was ausgeliefert wird == was committet ist“), deckt automatisch alle 11 Seiten ab und kann durch eine Copy-Änderung nie wieder driften. (2) Alarmpfad nach anyplots Muster: `issues: write`, `if: failure()`-Schritt, der ein Issue mit fixem Titel anlegt oder kommentiert, `if: success()`-Schritt, der es wieder schließt. (3) `docs/reference/crawler-richtlinie.md` §3 um den Satz ergänzen, dass die Titel aus `app/prerender/` kommen. Danach einmal `workflow_dispatch` und grün sehen.

### 12. [hoch] Der Codecov-Badge im README zeigt 87 % — ehrlich sind ~59 %, weil die SPA nur über importierte Module gemessen wird

- **Bereiche:** repo-hygiene · **Aufwand:** S · **KI jetzt** · **Owner:** Badge-Form: eine ehrliche Gesamtzahl (~59 %) oder zwei Flag-Badges (backend 88 % / frontend 18 %) — und welches frontend-Patch-Ziel danach in codecov.yml gilt.
- **Warum:** `app/vite.config.ts` hat keinen `test`-Block, Vitest 4 misst also nur, was ein Test importiert. Gegenprobe: `npx vitest run --coverage` → Lines 82,66 % (1059/1281); mit `--coverage.include='src/**'` → 18,08 % (1059/5857). Codecov bestätigt den Ausschnitt (43 statt 201 Frontend-Dateien). Repo-Totals: 8435/9667 = 87,25 %, daraus Backend 87,9 %, ehrlich kombiniert 59,2 %. Für ein Repo, dessen erklärter Zweck „Vorzeige“ ist, steht damit eine falsche Zahl an der prominentesten Stelle — und sie ist von jedem nachprüfbar, der das Repo ernst nimmt.
- **Vorschlag:** In `app/vite.config.ts` einen `test.coverage`-Block ergänzen (provider v8, `include: ['src/**/*.{ts,tsx}']`, `exclude: ['src/**/*.test.*','src/main.tsx','src/vite-env.d.ts']`) — in Vitest 4 der Ersatz für das alte `all:true`. Danach `codecov.yml`: das frontend-Patch-Ziel bleibt `auto`, die Projekt-Statuszeile braucht eine neue Basis, sonst fällt der nächste PR durch den 1-%-Threshold. Ein Satz in `docs/reference/frontend-stack.md` und ein Changelog-Fragment. Wenn die 59 % nicht auf der Startseite stehen sollen, ist die ehrliche Alternative ein Badge mit `?flag=backend` plus einem zweiten mit `?flag=frontend` — nicht das Zurückdrehen der Messung.

### 13. [hoch] Wizard: ein ungespeicherter Weg verschwindet ohne Nachfrage, und auf einer gesperrten Glyphe kommt nach getaner Arbeit eine rohe englische API-Zeile als „info“

- **Bereiche:** admin-harness · **Aufwand:** M · **KI jetzt** · **Owner:** Nur für (b) die Sperr-Doktrin: soll der Wizard eine gesperrte Glyphe gar nicht erst zum Zeichnen anbieten, oder mit sichtbarem Schloss plus bewusstem „Trotzdem überschreiben (force)“ — so wie es die Diagnose heute schon tut? Teil (a) und die Fehlerschicht brauchen keinen Entscheid.
- **Warum:** Zwei Befunde an der EINEN Fläche, an der nur der Autor arbeiten kann und an der Handarbeit Grundwahrheit erzeugt. (a) `SetupWizard.tsx:144` `<Dialog onClose={onClose}>` ohne `disableEscapeKeyDown`, ohne Backdrop-Guard, ohne Dirty-Prüfung; zweimal reproduziert: Zug gezeichnet → Escape bzw. Backdrop-Klick → Zug weg, kein Hinweis, kein Undo, kein Entwurfsspeicher. Das ist Datenverlust genau bei dem Schritt, den niemand sonst nachholen kann. (b) Auf `longs` (gesperrt) ist „Einrichten“ voll aktiv, der Kopf zeigt kein Schloss, nach dem gezeichneten Zug und „Weg speichern“ steht wörtlich „Error: 423 Locked: glyph 'longs' is locked; pass force=true to overwrite“ — in einem als `info` eingefärbten Kasten. Repo-weit rendern 18 Admin-Stellen `String(err)` roh.
- **Vorschlag:** (a) `useWizard` gibt `dirty` heraus (`strokes.length > 0` plus nicht übernommene Masken-/Lineatur-Änderungen); `onClose={(_e, reason) => { if (dirty) { setConfirmClose(true); return; } onClose(); }}` deckt `escapeKeyDown` und `backdropClick` gleichermaßen ab, der Footer-Knopf ruft denselben Pfad; Bestätigungsdialog nennt, was verloren geht; als Netz `strokes` in `sessionStorage` unter `kurrentschrift.wizard.<sourceId>.<glyphKey>` und beim nächsten Öffnen als „Nicht gespeicherter Zug von vorhin — wiederherstellen?“. (b) Sperrzustand VOR der Arbeit zeigen (`bboxesByKey[glyphKey].locked` liest der Wizard schon) mit Direkt-Knopf auf den Sperr-Umschalter; `SetupWizard.tsx:198` bekommt eine Severity aus dem Snack-Typ statt fester `info`; deutsche Fehlerschicht `apiFehlertext(err)` in `app/src/lib/api/` (Status → Satz), das englische Rohdetail als aufklappbares `<details>` darunter, alle 18 Stellen umgestellt.

### 14. [hoch] Der Compose-Pfad ist ungeschützt: kein Limiter, `ingress=all`, und ein rotierender Origin-Header umgeht den Edge-Cache

- **Bereiche:** api, security, perf-kosten, daten-lizenz · **Aufwand:** M · **KI jetzt** · **Owner:** Cloudflare-Regeln und ein neues Secret sind prod-berührend; außerdem der Entscheid, ob die öffentlichen /write-Reads credential-frei werden dürfen (ändert einen dokumentierten Client-Vertrag samt Hero-Preload). Der In-Process-Limiter kann ohne Rückfrage gebaut werden.
- **Warum:** Drei Befunde, die einander erst gefährlich machen. (1) Ein eindeutiger 155-Zeichen-Text kostet live 0,80 s TTFB und 1.653.798 Bytes; `api/request_context.py:4-6` hält selbst fest, dass es keinen Limiter gibt; `--concurrency=15 --max-instances=3`. (2) Beide Cloud-Run-Dienste stehen mit `ingress=all` direkt im Netz — die direkte `*.run.app`-URL antwortet ohne einen einzigen `cf-`-Header, jede Cloudflare-Maßnahme ist mit einer URL umgehbar, und die ADMIN_TOKEN-Break-Glass-Tür steht dort ohne Ratenbegrenzung offen. (3) Am Edge erzwingt ein rotierender `Origin`-Header MISS auf JEDER öffentlichen Read-URL (dreimal gemessen: zufälliger Origin → 3× MISS, konstanter Origin → 3× HIT) — ein Aufrufer braucht nicht einmal eindeutige Texte. Damit gilt die Annahme „Browser + Edge absorbieren Wiederholverkehr“ nur für gutwillige Clients, und dasselbe Loch ist die einzige offene Flanke der GPL-Begründung für `/lesarten`.
- **Vorschlag:** Der In-Process-Token-Bucket wird damit zur PRIMÄREN Maßnahme, nicht zur zweiten Schicht: pro Client-IP (rechtester XFF-Eintrag, anyplot-Muster in `api/request_context.py:8-36`) für `/write/word*`, 429 mit `Retry-After`, `/write/glyphs` und Einzel-Reads ausgenommen; er wirkt auf BEIDEN Wegen. Dazu, je nach Owner-Entscheid: eine Cloudflare-Rate-Limiting-Regel (60/min/IP auf `/sources/*/write/*`, im Free-Plan eine Regel frei) und/oder ein geteiltes Origin-Geheimnis per Transform-Rule, das die run.app-Umgehung schließt und die Break-Glass-Tür bewusst offen hält. Für den Cache-Key entweder eine Cloudflare-Cache-Rule, die `Origin` ignoriert, oder credential-freie öffentliche `/write`-Reads (`credentials: 'omit'` + statisches ACAO, dafür kein `Vary: Origin`). Das tatsächliche Verhalten in `docs/reference/write-api.md` und `frontend-stack.md` festhalten — heute setzt jede Maßnahme auf einer falschen Annahme auf.

### 15. [hoch] Vier Skills sind wörtlich ausgeführt unbrauchbar — und `/start` empfiehlt `alembic upgrade head` gegen die geteilte Prod-DB

- **Bereiche:** skills · **Aufwand:** M · **KI jetzt** · **Owner:** Nur für (c): ob ein Re-Export der Glyphbench-Fixtures überhaupt gewollt ist — bis dahin schreibt der Skill das Verbot.
- **Warum:** (a) `/verify-api`: vier von sechs Sweep-Zeilen schlagen live fehl — `/hands` → 401 (seit 2026-08-28 admin-gated, der Skill erwartet `[]`), `/diagnostic` → 401 (steht im Abschnitt „safe“), `n-medial` → tot seit Migration 0017 (fünf Zeilen). (b) `/verify-migrations` ist auf dieser Maschine gar nicht lauffähig (weder Docker noch System-Postgres), obwohl CLAUDE.md sie als Pflichtgate vor jedem Alembic-Push führt; der anyplot-Weg über `pgserver` funktioniert und wurde vom Prüfer komplett grün durchgefahren. (c) `/optimize-glyphs` Zeile 109 stürzt wörtlich ausgeführt mit `KeyError: "no fixture 't-medial'"` ab, und §1 lädt zum stillen Re-Export der eingefrorenen Fixture-Wurzel ein — ein kompletter Namensraumwechsel, der jede Baseline-Historie unvergleichbar machte. (d) `.claude/commands/start.md:35` empfiehlt ohne jede Warnung `uv run alembic upgrade head`, während `alembic/env.py:20` `load_dotenv()` ruft — der Alltagseinstieg zeigt damit auf die geteilte Cloud-SQL-DB; genau die Klasse, die laut `/verify-api`-Gotcha „einmal Prod umgelegt hat“.
- **Vorschlag:** Vier kleine, unabhängige PRs auf `.claude/`: (a) `n-medial` → `n` in fünf Zeilen, `/hands` und `/diagnostic` von §2 nach §3 als zweite 401-Sonde, Gotchas korrigieren, „quiz-words ≈ 500“ → ≈ 640, und den Sweep an `tests/test_api_public_surface.py` binden. (b) pgserver-Startweg als erster Weg (Verzeichnis `/var/tmp/…`, weil der Scratchpad-Pfad den 107-Zeichen-Socket sprengt), §0 „.env-Trap“, `alembic heads` als vierte Prüfung, die zwei anyplot-Gotchas, plus ein Absatz „vor einer Revision mit DROP/Rewrite gehört ein `tools/dbsnapshot` davor“. (c) Zeile 109 auf `t-final l-final b-final g-final`, §1 vom „Re-Export empfehlen“ auf „Re-Export verbieten, weil deklarierte Re-Baseline“ umschreiben, `qualitaetsmetrik.md` als Pflichtlektüre nennen. (d) start.md:35 durch „Nicht hier migrieren — die DB ist die geteilte Cloud SQL; Schemaänderungen laufen über den `kurrentschrift-migrate`-Job. Lokal prüfen: `/verify-migrations`“ ersetzen, dazu `npm install --no-audit --no-fund`.

### 16. [hoch] Die 92 Rohbefunde dieses Audits liegen ungetrackt im Arbeitsbaum — ein `git clean -xfd` löscht sie restlos

- **Bereiche:** triage, docs · **Aufwand:** S · **KI jetzt**
- **Warum:** `git status --porcelain docs/notes/` → `?? docs/notes/audit-2026-09-02-rohbefunde.md` (209 KB, 708 Zeilen, 92 Befunde aus acht Prüfern plus 54 Arbeitspaket-Zeilen), nicht ignoriert, nur nicht committet. CLAUDE.md benennt die Gefahr wörtlich („das Archiv lebt AUSSERHALB des Arbeitsbaums, weil `git clean -xfd` gitignorierte Dateien löscht“). Das Schwesterrepo hat die passende Ablage längst: `/home/tirao/anyplot/agentic/audits/` ist getrackt, sechs datierte Berichte plus `latest.md`. Solange nichts committet ist, ist ein einziger Aufräumbefehl genug, um den ganzen ersten Audit-Tag zu verlieren.
- **Vorschlag:** Doc-PR über `/write-docs`: (1) die Datei committen, nach anyplots Muster als `docs/notes/audit-2026-09-02-<bereich>.md` bzw. gebündelt, plus ein `docs/notes/audit-latest.md` als Einstieg; (2) `docs/index.md` bekommt Baum- und Prosa-Zeile für das Audit-Verzeichnis; (3) den Konventions-Absatz „Audit-Berichte werden committet, jeder Befund bekommt entweder ein Issue, eine Todoist-Aufgabe oder eine Korb-Zeile“ in `.claude/skills/write-docs/SKILL.md`; (4) die Bereichs-Prompts der Audit-Läufe analog zu anyplot unter `.claude/` versionieren, damit ein Folge-Audit denselben Zuschnitt fährt. Fragment `changelog.d/audit-berichte-committen.md` oder Label `skip-changelog`. Bis dahin: KEIN `git clean` in diesem Baum.

### 17. [hoch] Das Register der Mess-Kampagne ist zerfallen: §14 hat keinen Index, die Verfahrensseiten hinken zwei Adoptionen hinterher

- **Bereiche:** journal, docs · **Aufwand:** M · **KI jetzt**
- **Warum:** §14 trägt 74 Einträge über 6.190 Zeilen und ~47.000 Wörter; 52 von 74 Titeln heißen „Vorregistrierung“, obwohl 48 davon die Messung schon enthalten; 22 Nachträge sitzen in älteren Abschnitten; die Reihenfolge ist nicht mehr chronologisch (LF9 vor LF7/LF8). Das dafür gebaute Register ist nicht nachgezogen: `verfahren.md` meldet Kette v3, der `index.md`-Baum v4, `verfahren-kette.md` selbst v5; die Lotse-Seite kennt L-U und LF3b-W nicht, InkSight/Nullprobe haben keine L-U-Zeile — obwohl `index.md:181` die Ledger-Zeile im selben PR verlangt. Ergebnis: der aktuelle Stand der Kampagne ist ohne Lektüre von 6.200 Zeilen nicht ermittelbar; das Journal ist nur noch für den benutzbar, der es geschrieben hat. Genau das macht Delegation an KI-Sitzungen teuer.
- **Vorschlag:** (1) Registertabelle als ERSTER Block von §14 (Datum · Route · Arm · Typ · Verdikt in einer Zeile · Anker-Link) — Inhalt liegt als 74-zeilige CSV im Scratchpad des Journal-Prüfers; Titel NICHT umschreiben, die Anker werden 348-mal zitiert. (2) Zweite Tabelle: Headline-Ledger (Datum · PR · Wurzel/Re-Baseline · Wörter · Paare · Anker), heute nur verstreut im Fließtext. (3) Verfahrensseiten-Sync aug20→sep01: Übersicht auf v5, Lotse-Ledger um L-U und LF3b-W, InkSight-Stand „T0, auf Lineal 1,5 unvermessen“, Nullprobe-Zeile, Kette-Tabelle reparieren, `index.md:104-105`. (4) `tintenfolger.md` bekommt einen datierten „Stand der Kampagne“-Blockquote (Entwurf liegt vor) und einen neuen §7.11 „Offene Arme“ (Tabelle ohne Zahlen), außerdem die vier fehlenden §7.9-Zeilen für die Lotse-Negative vom aug16. (5) Durchsetzung: `tools/docs_register check` nach dem Muster von `tools/changelog check`, als CI-Job neben „Changelog (fragment)“.

### 18. [hoch] `tools/pairlab`s „exakte Mathematik des Produktions-Connectors“ ist seit dem 11.07. eingefroren — der echte Join-Block wurde dreimal umgebaut

- **Bereiche:** tools, geschriebenes · **Aufwand:** M · **KI jetzt** · **Owner:** Nachziehen vs. umdeklarieren — erst nach Vorliegen des Sensors zu beantworten; Nachziehen wäre eine deklarierte Re-Baseline der Duell-Kette.
- **Warum:** `tools/pairlab/analyze.py:237-258` behauptet im Docstring, „the exact maths of `core.compose.compose_word`'s join block (same constants, same guards)“ zu sein; letzte Änderung dieser Zeilen: 2026-07-11. `core/compose.py:1221 _connector_centerline` wurde danach dreimal umgebaut (#308, #358, #366), hat heute 18 Parameter und verzweigt nach Girlande, Gabel und Absatz-Ritt — der Spiegel hat 22 Zeilen, keinen dieser Zweige und emittiert immer die taute Einzel-Kubik. Numerisch: bei identischer Ein-/Ausgangsgeometrie liegen beide Kurven im Median 0,1215 xh, maximal 0,2985 xh auseinander (tiefster Punkt −0,2945 gegen −0,0226 xh), während das Empfindlichkeitsfenster des Wort-Lineals bei 0,05–0,12 xh liegt. Zwei Verbraucher: der GEMESSENE Gegenstand der Übergänge-Sektion und die Init-Schicht der Kette — genau die Schicht, an der LF3b-W an einer Kreuzung scheiterte.
- **Vorschlag:** Zweistufig, ohne eine Zahl zu bewegen. (1) SENSOR ZUERST: `tests/test_pairlab_connector_parity.py`, das über die Slots der Golden-Wörter (`tests/fixtures/compose_golden.json.gz`) für jeden Join einmal `_generate_connector` und einmal den Produktions-Connector aufruft und die Klassen ausdruckt, in denen sie auseinanderlaufen, mit max./Median-Abstand in xh — der Befund als Kennzahl statt als Behauptung, Laufzeit Sekunden. (2) Danach die Weiche: nachziehen (Aufwand L, `_connector_centerline` braucht ein Dutzend Kontextflaggen, die pairlab an der unabhängigen Platzierung erst rekonstruieren müsste — und es verschiebt den Init der Kette, also eine deklarierte Re-Baseline) ODER ehrlich umdeklarieren: Docstring auf „die taute Kubik, der Stand vor der Girlanden-Grammatik (#177)“, datierte Fußnote in `uebergaenge-befund.md`, `chain.py`-Init unangetastet (er ist per Docstring kein Ziel).

### 19. [mittel] Der Verbinder knickt am Austritt: im Median 13,6° steiler als das letzte Buchstabensegment, 82 % der Joins über 10°

- **Bereiche:** geschriebenes, core · **Aufwand:** M · **KI jetzt** · **Owner:** Die Adoption bricht das Golden (Silhouette ändert sich) und braucht damit ein Go für die deklarierte Re-Baseline; Sensor und trockene Messung nicht.
- **Warum:** 40 generierte Verbinder aus 13 Live-Payloads, Richtung über ein 0,05-xh-Fenster: Abgang Δ Median +13,6° (|Δ| 17,0°), Ankunft Δ −6,8°. Ursache: `core/compose.py:1221` kennt nur `entry_trim` (B-Seite); A-seitige Stub-Schnitte gibt es nur für LOOP_EXIT, KRINGEL_EXIT und BAR_EXIT — der Sägezahn-Austritt von e/n/m/u/i behält den Chart-Stub, dessen Fenstertangente flacher liegt als die Sehne zum Kopplungspunkt. §14 J1 sagt es selbst: „die Austrittsseite bleibt unangetastet“ — nie gemessen. Ehrlicher Gegenbefund derselben Prüfung: auf Lauf-Ebene sind die komponierten Diagonalen so gerade wie die Hand-Nachfahrung (Bogen 0,047 gegen 0,046 xh) — der Defekt ist lokal, unter dem Fenster des Lineals, aber im Zoom sichtbar.
- **Vorschlag:** Erst der Sensor, dann der Arm. Sensor: Report-Spalte `seam_deg` (Abgang/Ankunft-Δ je Join) in `tools/wordbench/run.py`, Kopfzahl byte-identisch. Arm „Austritts-Kollinearität“ (Stufe join_rule): für Sägezahn-Austritte im ALIGN_TAN_DEG-Band unter HIGH_COUPLE_EXIT_Y den A-Stub bis zur Fußwende zurücktrimmen (Spiegel von `entry_trim`, auf Centerline UND Silhouette via `erase_silhouette_piece`) und die Gerade von dort durch den bestehenden Kopplungspunkt legen; Platzierung UNANGETASTET. Gates: word_loss/pair_loss ≤ +0,002 · `dconn` fällt in ≥ 60 % der Klassen-Joins · `doff` byte-gleich · Kompositions-Soll ohne Verlust · seam_deg-Median der Klasse < 5°. Kill: dconn steigt → verworfen, Rettungsweg dann nur die Ankunftsseite als eigener Arm.

### 20. [mittel] Der gerenderte Strich ist ~25 % dünner als die Platte (0,145 gegen 0,194 xh) — und kein Lineal trägt Strichbreite

- **Bereiche:** geschriebenes · **Aufwand:** S · **KI jetzt** · **Owner:** Entscheidung über das öffentliche Erscheinungsbild (fetterer Strich) nach der Paarrunde — Messung und Schalter sind trocken.
- **Warum:** EDT auf dem Skelett der eingefrorenen Referenzmasken über 16 Wörter: Median-Strichbreite 0,194 xh (q25–q75 0,18–0,21). Gerendert wird 0,145 (Verbinder) bzw. 0,1595 (Buchstaben) aus dem chart-gepoolten Nib. In jeder Montage ist die Platte sichtbar fetter. Blind sind alle drei Lineale: `word_metric` ist Centerline-Chamfer plus Gesamtbreite, die §5-Metrik rein geometrisch, tracebench misst Bahnen — keine Kennzahl trägt Strichstärke. Das ist der zweitgrößte Eindrucksunterschied nach dem Zickzack und betrifft jede öffentliche Schreibfläche.
- **Vorschlag:** Pro Quelle einen `running_nib` aus dem EDT-Median der Wortproben ableiten (0,097 Halbbreite) und als zweiten Resolver-Wert neben dem Chart-Nib führen (Feld an `sources`, Default = heutiger Pool → byte-identisch, Schalter zunächst aus). A/B ausschließlich über die humanbench-Paarrunde mit der Echtheitsfrage (Rang 10), weil jedes Lineal neutral bleibt — vorregistrieren: ≥ 60 % „echter“ für den Platten-Nib, sonst verworfen. Nebenprüfung, ob die Ink-Clearance-Platzierung (`INK_CLEARANCE 0,14`) mit dem breiteren Strich Berührungen erzeugt (`gleichzug_doublings` darf nicht steigen). Golden bleibt (pen=None-Pfad), Wordbench byte-gleich.

### 21. [mittel] Kein sichtbarer Fokusring auf den Quiz-Antworten und allen MUI-Buttons; dazu Kontrast- und Typo-Boden-Verstöße auf vier Flächen

- **Bereiche:** live-desktop, live-mobile, app · **Aufwand:** M · **KI jetzt** · **Owner:** Zwei Geschmacksfragen: durchgehende Unterstreichung im Fließtext oder nur Farbwechsel (Briefton-Leitsatz), und ob die 44-px-Touch-Regel als bindend in design-system.md §9 aufgenommen wird.
- **Warum:** Mit echten Tab-Anschlägen auf die erste Quiz-Antwort: `outline-style: none`, derselbe Schatten wie die unfokussierten Nachbarn, Screenshot pixelgleich — das Quiz ist per Tastatur nicht bedienbar. Nur FÜNF Dateien im ganzen `app/src` definieren überhaupt focus-visible-Regeln; Lighthouse sieht es nicht (`focusable-controls` ist dort `manual`, /quiz steht trotzdem auf a11y 100). Dazu, aus demselben Theme-Grund: Landing-CTA „Lesen“ 3,72:1 (einziger Lighthouse-Fail der Startseite), /tafel „Lesetafel als PDF“ 3,28:1 bei 13 px, Fließtext-Links 1,35:1 gegen die Prosa ohne Unterstreichung (auf /schriftkunde betrifft das das komplette Inhaltsverzeichnis und alle Quellenangaben), drei Stellen unter dem bindenden 14-px-Boden (12,16 px Specimen-Bildunterschriften, 13,6 px Landing-Marken, 13 px MUI-small-Chips), und eine Reihe von Bedienelementen zwischen 19 und 31 px Höhe.
- **Vorschlag:** Eine Theme-Runde statt zwölf Einzelfixes: `components.MuiButtonBase.styleOverrides.root` um `'&.Mui-focusVisible': { outline: 2px solid viridian, outlineOffset: 2 }` — trifft Button, IconButton, ToggleButton, Chip und jedes eigene ButtonBase auf einmal; `MuiChip.labelSmall` und `MuiButton.sizeSmall` auf 0,875rem (hebt Federprobe-Chips, „Link kopieren“ und die Umschaltgruppen über den Boden, ohne jede Aufrufstelle anzufassen); `MuiLink.defaultProps.underline` auf `always` und Link-Farbe auf `paper.viridianText` (5,15:1); Landing-CTA `fontWeight: 600` (19,2 px fett = large text) oder Fläche auf viridianText; `specimenCaptionSx` und die drei Landing-Marken auf `variant="caption"`; gemeinsame `hitArea`-sx-Hilfe für ReplayButton, InfoHint, „beenden“, „Einstellungen ändern“. Danach Tastatur-Durchgang über /quiz, /federprobe, /schreiben/uebungsblatt, /tafel?g=n mit Vorher/Nachher-Screenshots und ein eincheckbares Messskript `app/scripts/type-floor.mjs` als Dauergitter; `design-system.md` §9 um die Fokus-, Link- und Touch-Regel ergänzen.

### 22. [mittel] Die Seite verspricht drei Dinge, die sie nicht hält: Quiz-Prerender, „erklärt jeden Fehlgriff“, und 1941 sind Erlass und Rundschreiben vertauscht

- **Bereiche:** website-content · **Aufwand:** S · **KI jetzt**
- **Warum:** (a) `app/src/lib/seo/prerender.ts:428-436` rendert Kurrent · Sütterlin · Offenbacher und drei Schwierigkeitsstufen als Auswahl; die SPA verbirgt sie seit #447 bewusst, weil es sie nicht gibt. Crawler und KI-Antworten lesen damit ein Kurrent-Quiz mit drei Handschrift-Stufen — im Widerspruch zum eigenen Titel „Sütterlin-Quiz“ und zur Ehrlichkeitslinie der Landing. (b) `locales/de/hub.ts:134` verspricht „erklärt jeden Fehlgriff“; tatsächlich erklärt `QuizPlayPanel.tsx:72-73` nur Buchstaben und nur die 19 dokumentierten Paare — der Code sagt es selbst: „no explanation is better than an invented one“. (c) `schriftkunde.ts:256` stellt Erlass und Rundschreiben von 1941 vertauscht dar, gegen das eigene Faktenblatt UND gegen die eigene Chronologie drei Absätze weiter — auf einer Seite, die „Jede Angabe auf dieser Seite ist belegt“ behauptet.
- **Vorschlag:** (a) `quizBody` auf dieselbe Regel wie die SPA stellen: SCRIPTS/MODES/DIFFICULTIES aus `quizTypes.ts` importieren (React-frei) und nur Zeilen rendern, für die `offersChoice` gilt; nicht wählbare Fakten als Satz; danach `npm run prerender` und den Drift-Guard `prerender.test.ts` um „keine unavailable-Option im Quiz-Body“ erweitern. (b) hub.ts:134 auf die belegbare Zusage umschreiben („nennt dir bei den klassischen Verwechslern — n und u, e und n, ſ und f — das Merkmal“). (c) schriftkunde.ts:256/257 nach dem Faktenblatt korrigieren (Rundschreiben der Parteikanzlei 3. Januar, Erlass des Reichserziehungsministeriums zum 1. September). Alle drei mit `sitemap.xml`-lastmod-Bump und frischem Prerender — die lastmods sind ohnehin bis zu zwei Wochen alt und schlagen als falsche „Stand“-Zeile in die Crawler-Seiten durch.

### 23. [mittel] Der deploy-api-Trigger ignoriert `app/prerender/` — Crawler bekommen bis zum nächsten zufälligen API-Push die alten Seiten

- **Bereiche:** ci-deploy · **Aufwand:** S · **braucht Owner** · **Owner:** Der `gcloud builds triggers update` ist GCP-Schreibzugriff und prod-berührend — Freigabe oder Ausführung durch den Autor. Die Doc-Teile brauchen keinen Owner.
- **Warum:** Der Trigger listet `api/**, core/**, alembic/**, data/sources/**, pyproject.toml, uv.lock, …`; das API-Image kopiert aber laut `api/Dockerfile:80/85` `tools/quizgen/quiz_words.json` und `app/prerender/`, und die Crawler-Seiten liefert der API-Dienst über `/seo-proxy` aus. Acht Commits seit dem 29.08. haben `app/prerender/` geändert, ohne einen deploy-api auszulösen — für jeden davon sahen Menschen die neue Kopie und Crawler die alte. Dass es aktuell stimmt, ist Zufall (die letzten PRs haben api/ berührt), keine Garantie. Die dahinterliegende Regel fehlt überall: die `includedFiles` eines Triggers müssen die COPY-Liste seines Dockerfiles abdecken.
- **Vorschlag:** (1) `gcloud builds triggers update` auf deploy-api: `includedFiles` um `app/prerender/**` und `tools/quizgen/quiz_words.json` erweitern. (2) Damit es nicht wieder auseinanderläuft: den Pfadfilter als Kommentarblock neben die COPY-Zeilen in `api/Dockerfile` und als eigene Zeile in `docs/reference/frontend-stack.md` §Deploy (dort steht heute nur „je ein Trigger pro Service“). (3) Im selben Doc-PR die Zeile „Max instances: API 3, App 3“ auf den Ist-Stand (App 1) korrigieren und den Rollback-Pfad ergänzen, den heute kein Dokument nennt: `gcloud run services update-traffic kurrentschrift-api --region=europe-west4 --to-revisions=<vorherige>=100`.

### 24. [mittel] Vier Dependabot-PRs liegen seit zwei Tagen an einem veralteten Pflicht-Check fest, dessen Ausnahme längst gemergt ist

- **Bereiche:** ci-deploy, repo-hygiene, triage · **Aufwand:** S · **KI jetzt** · **Owner:** Der Merge der vier Bumps (der Autor merged PRs live).
- **Warum:** #465–#468 sind alle MERGEABLE, aber BLOCKED: Backend, Frontend und Migrations grün, „Changelog (fragment)“ rot. Ihre Läufe starteten am 31.08. um 10:14–10:16 UTC; der Fix, der Dependabot vom Gate ausnimmt (#469), wurde um 12:30 UTC gemergt — zwei Stunden später. Der `if`-Ausdruck ist korrekt, er ist bei diesen vier Läufen nur noch nicht gelaufen. Vier offen liegende Bumps direkt neben dem README sind für einen Repo-Leser das erste Zeichen eines schlecht gepflegten Projekts — und einer davon (#468) ist genau der python-minor-Bump, der einen Teil der Advisories aus Rang 7 mitbrächte.
- **Vorschlag:** (1) Auf jedem der vier PRs `@dependabot rebase` kommentieren — das ist der verlässliche Weg, weil ein `gh run rerun` denselben Merge-Commit und damit das alte Workflow-File benutzt, während ein Rebase einen neuen Merge-Commit gegen das aktuelle main erzeugt, in dem der Job übersprungen wird und als bestanden zählt. Danach grün melden; das Mergen bleibt beim Autor. NIE `update-branch` auf einem Dependabot-Branch (anyplots dokumentierte 174-Runs-Falle). (2) Dauerhaft: `.claude/skills/dependabot/SKILL.md` aus anyplot übernehmen und auf das Fragment-Verfahren hier anpassen.

### 25. [mittel] 121 von 129 Remote-Branches gehören zu bereits gemergten PRs — `delete_branch_on_merge` ist aus, in anyplot an

- **Bereiche:** repo-hygiene · **Aufwand:** S · **braucht Owner** · **Owner:** Das Setting und die Freigabe der Löschliste — eine Massenlöschung von Remote-Branches ist nicht folgenlos zurückzunehmen.
- **Warum:** `git branch -r` → 129; Abgleich gegen 435 gemergte PRs → 121 Branches, deren PR gemergt ist, Median 13 Tage alt, wachsend mit ~4 pro Tag. Nicht aus einem gemergten PR stammen nur acht (main, vier Dependabot, zwei Arbeitsbranches, #474). Ursache: `delete_branch_on_merge: false`; anyplot: true. Die Branch-Liste ist das Zweite, was ein Besucher nach dem README sieht, und 121 Leichen sagen etwas anderes über die Pflege des Projekts als der Rest des Repos.
- **Vorschlag:** (1) Setting umlegen (`gh api -X PATCH repos/MarkusNeusinger/kurrentschrift -f delete_branch_on_merge=true`) — danach räumt es sich selbst auf. (2) Einmal-Aufräumen der 121 Altlasten mit drei Schutzregeln: nie einen Branch löschen, der in `git worktree list` steht (aktuell vier, darunter `laufform-lf10-form-abstand` von PR #474), nie einen ohne gemergten PR, und die Liste vor dem Lauf ausgeben statt blind zu löschen — die fertige Liste liegt im Scratchpad des Repo-Hygiene-Prüfers. Im selben Zug: Merge-Commits und Rebase abschalten, damit die Schaltfläche beim Mergen dieselbe Welt zeigt wie die Squash-Rezepte in CLAUDE.md.

### 26. [mittel] /write/word bricht den eigenen 4-Dezimal-Vertrag: 36 % der Zahlen tragen float64-Rauschen, das 22 % der Draht-Bytes kostet

- **Bereiche:** perf-kosten, api · **Aufwand:** S · **KI jetzt** · **Owner:** Nur die Ziffernwahl, falls 3 statt 4 gewünscht ist (−39,5 % statt −21,8 %, aber 1e-3 Vorlageneinheiten ≈ 0,2 px bei üblicher Renderhöhe). Ohne Antwort ist 4 die belegte Hauslinie.
- **Warum:** Von mir live nachgemessen: `/write/word?text=lesen` → 3.777 Zahlliterale, davon 2.185 mit vier Nachkommastellen und 1.352 (35,8 %) mit 14–20 (`0.015600000000000001`). Zum Vergleich hält `/write/glyphs` den Vertrag (2.893 von 3.209 auf vier Stellen). Ursache: `core/pipeline.py:515-557` rundet jede Payload-Zahl auf vier Stellen, `core/compose.py` rundet nirgends — Platzierung, Übergangserzeugung und Laufform-Skalierung rechnen die gerundeten Eingaben zu voller float64-Breite auseinander. Gemessene Ersparnis bei gzip-6: identity 46.440 → 30.570 Bytes (−34 %), gzip 13.864 → 10.840 (−21,8 %). `/write/word` ist mit 272 von 1.000 Anfragen die häufigste Origin-Route. Rekompression ist als Alternative ausgeschlossen (br 13.900, zstd 14.618 gegen gzip 13.864).
- **Vorschlag:** In `api/routers/write.py:60-66` (`_geometry_response`) vor `orjson.dumps` einen rekursiven 4-Dezimal-Walk — dieselbe Stelle, an der schon der Cache-Control-Header sitzt, also reine Serialisierung. `core/compose.py` und das Golden bleiben unangetastet. EINE Kopplung muss mit: `tools/wordbench/fetch_fixtures.py` vergleicht im `--verify`-Pfad lokal komponierte Fälle BIT-EXAKT gegen `GET /write/word` — dieselbe Rundungsfunktion gehört in den Vergleich der verify-Seite, sonst schlägt das Gate fehl. Vor dem Merge einmal mit `time` gegen die Ist-Route messen (der Walk über ~3.800 Zahlen liegt bei ein bis zwei Millisekunden gegen 52 ms p50 Compose-Zeit und wird durch die 34 % kleinere gzip-Eingabe voraussichtlich überkompensiert) und die Zahl in den PR schreiben; danach das Histogramm-Skript zur Kontrolle wiederholen.

### 27. [mittel] Kein Bytecode im API-Image — jeder Kaltstart kompiliert ~1.550 Module neu (≈ 2 s von ≈ 12 s)

- **Bereiche:** api, perf-kosten · **Aufwand:** S · **KI jetzt**
- **Warum:** `api/Dockerfile:34` fährt `uv sync --frozen` ohne `--compile-bytecode`, und `.dockerignore` schließt `__pycache__` aus, die COPYs bringen also auch keinen mit. Zwei unabhängige Messungen: ohne .pyc 3,09–4,16 s Import, mit .pyc 1,08–2,63 s. Der Perf-Prüfer hat den Kaltstart danach zerlegt: 40 Instanzstarts min 6,35 / p50 9,93 / max 16,06 s, davon liegt praktisch alles VOR uvicorns erster Zeile. Das ist der einzige Hebel mit belegter Zahl und zwei Zeilen Aufwand — und er setzt zugleich die ehrliche Decke für den geplanten Lazy-Import-Umbau: Bytecode plus Importe decken zusammen höchstens ~3 s von ~12 s ab, der Rest ist Cloud-Run-Scheduling und Image-Pull.
- **Vorschlag:** Im Builder `ENV UV_COMPILE_BYTECODE=1` vor `uv sync`; im Runtime-Stage nach den COPYs `RUN .venv/bin/python -m compileall -q api core alembic`. Danach `python -X importtime` im Container gegen die alte Zahl messen und die Kaltstart-Zeile in `api/cloudbuild.yaml:13-18` aktualisieren — dort steht heute die 30-Tage-Messung des FETTEN Images als Begründung für min=1, und dieselbe Zeile sollte bei der Gelegenheit ehrlich werden: min=1 hat die AUTOSCALING-Kaltstarts abgeschafft (23 in den fünf Tagen davor, danach null), aber nicht die Recycle-Starts (zwei am 01.09. mit 16,1 s und 11,0 s) — die Retry-Logik im Client bleibt also berechtigt. Schwesterregel: `/home/tirao/anyplot/api/Dockerfile` hat dieselbe Lücke. Den Lazy-Import-Umbau erst danach und erst nach einer Nachmessung ansetzen.

### 28. [mittel] Der Wiederholen-Knopf liegt in der Tinte — und auf dem Handy schreibt die Federprobe einen Satz 22 px hoch

- **Bereiche:** live-desktop, live-mobile · **Aufwand:** M · **KI jetzt** · **Owner:** Nur Stufe (c): welche der drei Strategien für lange Texte auf schmalen Geräten gilt — das berührt die „Zug um Zug“-Doktrin der Federprobe (eine Zeile = ein durchgehender Federzug-Verlauf).
- **Warum:** Programmatisch nachgemessen: auf /federprobe überschneidet das 30×30-Knopf-Rechteck die Bounding-Box des letzten Glyph-Pfades; im Quiz sitzt es mitten im 62,5 px breiten Glyph-Kasten und liest sich im Element-Screenshot wie ein Bestandteil der Letter — auf einer Seite, die Buchstabenformen genau lesen lassen will. Auf 360 px kommt der eigentliche Grund dazu: `WrittenWord.tsx:182-186` rechnet `finalH` gegen die AUFRUFER-Konstante `maxWidth` (840 bzw. 760), während die tatsächliche Breite über `maxWidth:'100%'` auf 286–316 px gedeckelt wird — die Box wird flach gerechnet, ein 29-Zeichen-Satz landet bei 7,1 px pro Einheit (Tintenhöhe 22,5 px) in einer 200 px hohen Karte, und der absolut positionierte Knopf landet zwangsläufig auf der Schrift. Die Federprobe ist die Schreibhand des Projekts; auf dem Handy ist sie damit unlesbar.
- **Vorschlag:** (a) Kollision beseitigen: `minHeight: height` am Wrapper in `WrittenWord.tsx:190` oder den ReplayButton unter die Tintenfläche setzen; die Verankerung generell vom Ink-Kasten auf den WEISSEN Rahmen umstellen (die Rahmen sind auf allen drei Flächen deutlich größer als die Tinte). (b) Höhe aus der ECHTEN Breite ableiten: die verfügbare Breite mit dem vorhandenen `useElementSize`-Hook messen und `maxWidth` daraus bilden statt aus der Aufruferkonstante — dann füllt die Zeile die Karte wenigstens vertikal. (c) Für lange Texte auf schmalen Geräten braucht es einen Autor-Entscheid (Umbruch in mehrere Zeilen · Maßstab-Boden mit horizontal scrollender Fläche · viewportgekoppelte Zeichenobergrenze mit ehrlichem Hinweis). Prüfung: die drei belegten Fälle nachstellen und die Rechteck-Überschneidung per `getBoundingClientRect` gegen null testen.

### 29. [mittel] „im Wort sehen“ fehlt bei über der Hälfte der Buchstaben, und die Quiz-Auswertung verlinkt die Verwechsler nicht auf die Tafel

- **Bereiche:** live-desktop · **Aufwand:** S · **KI jetzt**
- **Warum:** `letterDetail.ts:57` filtert das Beispielwort auf `era === 'modern' && !fugen` — das sind genau 22 Wörter der Bank. Ausgezählt bleiben OHNE Beispielwort: die Kleinbuchstaben j k p q v x y z ä ö ü ß (12 von 30) und 19 der 26 Großbuchstaben. Die Federprobe-Brücke, die PR #457 gerade erst gebaut hat, fehlt damit auf mehr als der Hälfte der Detailseiten. Gleichzeitig zeigt die Quiz-Auswertung „Häufig verwechselt“ und „Machte Mühe“ ausschließlich als Bilder und Text — über die ganze Quiz-Seite gibt es NULL Links —, obwohl `/tafel?g=<key>` seit #457 live funktioniert und für ein n genau das zeigt, was der Lernende nach einem Fehlgriff braucht. Die drei Werkzeuge stehen heute nebeneinander statt in einer Schleife.
- **Vorschlag:** (1) `exampleWord` um eine Rückfallstufe erweitern: findet sich kein modernes Bankwort, ein historisches (im Link als solches markiert), sonst eine kleine geprüfte Konstante `EXAMPLE_WORDS: Record<string, string>` neben `letterDetail.ts` (k → „Kind“, q → „Quelle“, z → „Zeit“, ü → „für“, ß → „Fuß“ …), die nur als Rückfall greift — damit bleibt die Bank die erste Quelle und das Verhalten deterministisch; Test „jeder glyph_key aus LETTERS liefert ein Beispielwort“. (2) In `QuizResultsPanel.tsx` die Karten von „Häufig verwechselt“ und „Machte Mühe“ zu `RouterLink`s auf `${paths.tafel}?g=${glyphKey}` machen (der Key liegt in den Auswertungsdaten schon vor) und darunter eine Zeile „Die Formen, die dir Mühe machten, Zug um Zug ansehen → Schreibtafel“. Kostet einen Link und schließt die Lernschleife Quiz → Tafel → Federprobe.

### 30. [mittel] /tafel: CLS 0,47 auf dem Desktop durch ungemessene Tafelscans, 0,10 mobil durch die späte GLKurrent-Initiale

- **Bereiche:** live-desktop, live-mobile · **Aufwand:** S · **KI jetzt** · **Owner:** Nur die Schau-Initiale: `font-display: optional` (erscheint auf langsamen Verbindungen gar nicht, dafür kein Sprung) oder garantiert erscheinen und über metrische Overrides sprungfrei machen.
- **Warum:** Zwei Ursachen, eine Route, der einzige Lighthouse-Fehlschlag der Seite. Desktop: alle drei `<img>` haben `width: null, height: null`, `aspect-ratio: auto` und `loading="lazy"`, obwohl die Maße bekannt sind (1633×1869, 1614×1300, 2190×1029) — beim Laden springt die Seite je Tafel um mehrere hundert Pixel. Mobil unter Slow 4G + 4× CPU: CLS 0,10, davon ein einzelner Shift von 0,0936 auf die Abschnitts-Initiale, weil `@font-face` für die reine Schau-Schrift GLKurrent auf `font-display: swap` steht und der `cursive`-Fallback deutlich anders breit ist. 0,10 liegt exakt auf der „Good“-Grenze und kippt auf einer langsameren Verbindung darüber.
- **Vorschlag:** (1) `width`/`height` bzw. `sx={{ aspectRatio: '1633 / 1869' }}` je Stil aus einer kleinen Konstante an die drei Grundtafel-Scans in `app/src/sections/tafel/`. (2) In `app/index.html` `font-display: optional` für 'GLKurrent' (die Initiale ist Schmuck — lieber gar nicht als nachträglich springend) oder metrische Overrides auf einer eigenen Fallback-Deklaration; zusätzlich `eb-garamond-latin-600-normal.woff2` in die Preload-Liste (Gewicht 600 trägt jede CategoryHeading und wird heute erst spät entdeckt). (3) Gegenmessung: Lighthouse auf /tafel desktop und ein Trace bei 390×844 unter Slow 4G + 4× CPU, Ziel CLS ≤ 0,05; die Zahlen in `frontend-stack.md` festhalten. Nebenbei mit derselben Bewegung: die Antiqua-Beschriftung unter jede Zelle der geschriebenen Tafel — heute sind 62 Zellen ohne sichtbaren Buchstaben, und die letzte (die Ziffer 0) ist von einem geschriebenen o nicht zu unterscheiden.

### 31. [mittel] Das README beschreibt ein Datenmodell, das es seit Migration 0017 nicht gibt — und die Community-Health steht bei 71 % gegen anyplots 100 %

- **Bereiche:** repo-hygiene, docs · **Aufwand:** S · **KI jetzt**
- **Warum:** `README.md:35`: „The library unit is `(glyph, position, variant)` — medial `s-lang` and final `s` are different glyphs“. CLAUDE.md und `architektur.md:146` sagen `(style, glyph, variant)`, und die Position ist seit `0017_position_removal.py` reiner Render-Kontext. Das README ist die Eingangstür und beschreibt als einziges Dokument ein Datenmodell, das es nicht mehr gibt — für einen fachkundigen Leser der schnellste Weg, dem Rest zu misstrauen. Dazu `gh api …/community/profile` → 71 %: kein CODE_OF_CONDUCT.md, kein `.github/pull_request_template.md`; anyplot hat beides und steht bei 100 %.
- **Vorschlag:** Ein kleiner PR: (1) README.md:35 auf `(style, glyph, variant)` ziehen, style als Grundvorlage/Schriftfamilie erklären, die Allographen-Aussage auf die heutige Formulierung bringen (langes ſ und rundes s sind getrennte Glyphen; die Wortposition ist Render-Kontext) und prüfen, ob `.github/copilot-instructions.md` dieselbe Passage trägt. (2) CODE_OF_CONDUCT.md aus anyplot (Contributor Covenant, Kontakt admin@kurrentschrift.ink — dieselbe Adresse wie SECURITY.md). (3) `.github/pull_request_template.md` mit anyplots Struktur, aber auf dieses Repo umgeschrieben: Fragment unter `changelog.d/` (nicht „CHANGELOG.md unter [Unreleased]“), `Fixes #N`, passendes `/verify-*`-Skill, neuer Fachbegriff im Glossar; Kommentarkopf nimmt Dependabot aus. (4) Im selben Zug `app/package.json`-description (nennt die App noch „admin UI — canonical extraction … with stylus input“) und pyproject-classifiers/urls.

### 32. [mittel] CI baut das Image nie, der Migrations-Job ist kein Pflicht-Check, und der App-Deploy geht ungeprüft auf 100 % Traffic

- **Bereiche:** ci-deploy · **Aufwand:** M · **KI jetzt** · **Owner:** Der Ruleset-Eintrag (Required Check) ist eine Repo-Einstellung und braucht Admin-Rechte.
- **Warum:** Drei Löcher in einer sonst vorbildlichen Kette. (1) `ci.yml` enthält kein `docker`, `buildx` oder `hadolint` — der erste Bauversuch eines geänderten Dockerfiles findet nach dem Merge statt. Konkret: in #473 fiel `pyproject.toml` aus dem Runtime-Stage, `_project_version()` wäre auf `0.0.0` zurückgefallen, und gefunden hat es der Copilot-Reviewer, nicht ein Gate — auch der Cloud-Build-Smoke hätte es durchgelassen, weil `/health` kein Versionsfeld liefert. (2) `Migrations (alembic upgrade head)` steht nicht in den drei Required Checks des Rulesets; er ist der EINZIGE Ort, an dem die Kette gegen echtes Postgres läuft (pytest fährt auf SQLite), und ein roter Lauf blockiert nichts, killt aber den Deploy in einem Log, das niemand abonniert hat. (3) `app/cloudbuild.yaml` hat weder `--no-traffic` noch Tag noch Smoke — der Dienst, der die gesamte Crawler-Logik in `app/nginx.conf` trägt (dieselbe Datei, deren Bruch bei anyplot vier Wochen lang jedem Bot ein 502 lieferte), geht ohne Zwischenprüfung live.
- **Vorschlag:** (1) Fünfter, paralleler Job `image` in ci.yml: `docker/build-push-action` mit `push:false, load:true` + GHA-Cache, dann ein Container-Smoke ohne DB — `/health` grün, `openapi.json`'s `info.version` == pyproject-Version (genau der Assert, der #473 gefangen hätte), `docker exec … test -f /app/app/prerender/index.html` (pinnt die COPY-Liste), plus hadolint auf beide Dockerfiles; ~4 min, Wall-Clock unverändert weil parallel. (2) Kontext `Migrations (alembic upgrade head)` ins Ruleset aufnehmen, `strict` bewusst auf false lassen. (3) `app/cloudbuild.yaml` 1:1 auf das API-Muster heben: `--no-traffic --tag=candidate --revision-suffix=b$BUILD_ID`, Smoke gegen die Kandidaten-URL (Human-UA → `<div id="root">`, Googlebot-UA → Prerender-Marker, `/robots.txt` enthält `User-agent: Bytespider`, `/llms.txt` charset), dann `update-traffic`; alle curls mit `--retry 5 --retry-all-errors`, weil die App immer kalt startet. Im selben PR `push-latest`/`push --all-tags` in BEIDEN cloudbuild-Dateien hinter `promote` ziehen, `timeout-minutes` je Job (gemessenes Maximum 491 s) und die acht `uses:`-Tags auf Commit-SHAs mit Versionskommentar.

### 33. [mittel] Drei Tafelformen widersprechen der Platte: der konkave Anstrich von t/ſ/k, die zu breite d-Schleife, die flache r-Fahne

- **Bereiche:** geschriebenes · **Aufwand:** M · **braucht Owner** · **Owner:** Wizard-Nachfahrungen (Autorenarbeit) — oder die ausdrückliche Erlaubnis, den Anstrich per Klassenregel grammatisch zu absorbieren; plus der seit dem 26.08. offene d-Auslauf-Entscheid (a)/(b).
- **Warum:** Gemessen an Strich 0 der Chart-Payloads: t weicht 0,181 xh von der Sehne ab (konkav, erstes Segment 38,8°, letztes 84,2°), ſ 0,136, k 0,187 — die Platte schreibt EINE Gerade vom Fuß bis zum Apex mit einem weißen Keil bis fast oben. Die d-Schleife ist 1,103 xh breit gegen ≈0,66 auf der Platte, und der Verbinder fällt aus der Schleifenkreuzung bei y 1,17 mit −34° ins a, während die reale Tinte die d-Säule bei y 0,82–1,12 (Median 0,96) verlässt und STEIGT — das trifft „das“, „die“, „und“. Die r-Fahne läuft als waagerechter Balken aus, wo die Platte eine steigende Welle schreibt. Der Korb-#7-Haken steht damit nach T0 weiter, obwohl J1/J2/J3 an ihren Gates verworfen wurden: die Autopsie legte den Haken auf die ZEILE, die Chart-Form selbst trägt aber denselben konkaven Kopf.
- **Vorschlag:** Nach Doktrin ist die Tafel Autorenarbeit, also drei Wizard-Nachfahrungen (Snapshot vorher): t/ſ/k mit geradem Anstrich bis zum Apex, d mit schmalerer und geneigter Schleife (Zielbreite ≈0,66 xh), r mit Wellen-Fahne — je mit den Platten „unter“, „das“, „Wer“/„Gewehr“ als Vorlage. AI-seitig vorher messbar und als Zielzahl in die jeweilige Todoist-Aufgabe zu schreiben: die Dissektion der 4 t- und 11 ſ-Vorkommen (Sehnenwinkel und Bogen des Hand-Anstrichs) sowie die Auslauf-Dissektion der drei wortfinalen r. Für den d-Auslauf steht die Weiche seit dem 26.08. offen — (a) Variantentrennung verbunden/terminal oder (b) Auslauf im Übergangsgenerator; bei (b) baut eine Sitzung LOOP_EXIT für d in gebundenem Kontext auf y ≈ 0,96 mit steigendem Verbinder, Gates word/pair ≤ +0,002 und fallendes `dconn` der d-Joins.

### 34. [mittel] Fünf Regeln stehen nur in copilot-instructions.md, vier Owner-Direktiven nur im lokalen Gedächtnis — und `prime.md` ist an fünf Stellen falsch

- **Bereiche:** anleitungen · **Aufwand:** M · **KI jetzt** · **Owner:** Nur zwei Kleinigkeiten: Go zum Archivieren der zehn erledigten Memory-Einträge, und ob neue Regel-Einträge ab jetzt „author directive“ statt „owner directive“ heißen (Anrede-Entscheid 2026-08-20, forward-only).
- **Warum:** Beide Anleitungen behaupten Synchronität, aber nur 42 % der Zeilen sind byte-identisch, und ganze Regelblöcke existieren nur in EINER Datei: „Never merge a PR yourself“, „core/-PRs zitieren Bench-Zahlen“, die Code-Standards (Typhints Pflicht, kein Redux/Zustand), „nie still abweichen“ und die Codecov-Regel kennt nur Copilot. Vier Owner-Direktiven leben ausschließlich in `~/.claude/…/memory/` und erreichen damit weder eine Cloud-Session noch Copilot: `Fixes #N` im PR-Body, die Sibling-Transfer-Regel, „asymmetrische Befunde nutzen“ und „der Autor autort im Prod-Admin“. Zwei Memory-Einträge widersprechen inzwischen dem Repo (min-instances 0/1 gegen 1/3 seit #455 — eine Sitzung mit Recall würde es „zurückfixen“). Dazu behauptet copilot-instructions „Pre-commit hooks: none configured yet“, obwohl `.pre-commit-config.yaml` seit #204 existiert, und `prime.md` nennt das Bibliotheks-Tupel noch mit `position`, 9 statt 18 Router, 8 statt 20 Modelle, 5 statt 19 Tool-Pakete und einen Wizard-Schritt, den es nicht gibt.
- **Vorschlag:** Ein Regel-Sync-PR: die fünf Copilot-only-Regeln als Einzeiler in CLAUDE.md „Working guardrails“ bzw. einen neuen Kurzabschnitt „Code standards“; den Pre-commit-Ist-Stand in beide Dateien; die vier Memory-Direktiven in beide Dateien (die Sibling-Regel ohne lokalen Pfad, als „das Schwesterrepo anyplot“), `Fixes #N` zusätzlich als Schritt in `.claude/skills/open-pr/SKILL.md`, die Asymmetrie-Regel zusätzlich in den Vorspann von `tintenfolger.md` §7.9, wo Messläufe sie brauchen; die Dateilisten in copilot-instructions durch Zeiger ersetzen („Tabellen: core/database/models.py“) — Listen rotten mit jedem PR, Zeiger nicht. `prime.md` auf den Run-Block kürzen (der ist aktuell und der eigentliche Mehrwert), die duplizierte Repo-Karte streichen. Als Wiederholungsschutz `tests/test_agent_instructions.py`: jeder Backtick-Pfad existiert, jeder „§N“-Verweis trifft eine Überschrift in der genannten Datei. Memory-seitig: `cost-min-instances.md` auf den Ist-Stand, `deploy-status-doc.md` umlenken, zehn erledigte Einträge unter eine Archiv-Überschrift.

### 35. [mittel] Zwei Laufform-Zeilen liegen unter dem Beweis-Boden, und das Korb-Protokoll lässt sich nach einer Zurückweisung mit leerem Status wiederholen

- **Bereiche:** werkbank-doktrin, api · **Aufwand:** S · **KI jetzt** · **Owner:** Entscheid über die zwei Zeilen P und s — ein rendering-wirksamer Prod-Schreibvorgang mit dbsnapshot davor.
- **Warum:** (a) Von den 22 Live-Laufform-Zeilen haben P und s je `n_occurrences: 1`, während `LAUFFORM_MIN_OCCURRENCES = 3` gilt; beide passieren Sprung- und Kopf-Gate, es trennt sie NUR der Boden, und `put_laufform` würde sie heute mit 422 abweisen. Das Zeilen-Gate schützt also Neuschreibungen, nicht den Bestand — und `tools/laufform/inventory.py` kennt nur zwei Flags („über τ“, „Kopf“), der Boden taucht in der Bestandsaufnahme nicht auf. Keine Zeile sagt, warum sie unter dem Boden liegt. (b) `api/routers/work_items.py:117` fällt für die Pflichtfelder auf `stored` zurück; nach einer Zurückweisung (die UI lässt `understanding`/`stage`/`resolution` bewusst stehen) genügt ein nacktes `{"status":"done"}`, um dieselbe, gerade zurückgewiesene Rückspiegelung wieder in Kraft zu setzen — die zweite Runde ist ungeschützt, obwohl §5.1 die Rückspiegelung genau dann erst recht verlangt.
- **Vorschlag:** (a) Drittes Flag „unter dem Boden“ in `print_table` von `tools/laufform/inventory.py` plus Summenzeile; in `api/routers/templates.py::put_laufform` bei gesenktem Boden `floor_lowered_to` ins Provenienz-Dict schreiben, das `build_laufform_canonical` ohnehin entgegennimmt — erst damit ist eine bewusste Autor-Aussage von einer Alt-Zeile unterscheidbar. Die zwei Zeilen P und s selbst sind Autorensache (löschen / aus mehr Vorkommen neu ableiten / als erklärte Ausnahme behalten) und gehören als Todoist-Aufgabe abgelegt. (b) Einzeiler: `missing = [f for f in required if _blank(changes.get(f))]` — der `stored`-Fallback hat keinen legitimen Anwendungsfall, weil Protokollfelder nur durch den Übergang schreibbar sind, der sie verlangt; drei Zeilen in die Parametertabelle von `tests/test_api_work_items.py` (zurückgewiesene Zeile + ack/done → abgelehnt; + vollständige Felder → erlaubt) und je ein Satz in Werkbank §5.1 und `.claude/skills/work-basket/SKILL.md`.

### 36. [mittel] Datenschutz: Rechtsgrundlage, Widerspruchs- und Beschwerderecht fehlen; die Cloudflare-Standortaussage ist ungesichert

- **Bereiche:** website-content · **Aufwand:** S · **braucht Owner** · **Owner:** Freigabe des Rechtstexts (keine Rechtsberatung möglich); Bestätigung, ob bei Cloudflare Regional Services/Data Localization aktiv ist; Entscheid zur ladungsfähigen Anschrift.
- **Warum:** `impressum.ts:490-522` nennt Verantwortlichen, Zwecke, Speicherdauer, Empfänger und den DPF-Transfer, aber nicht die Rechtsgrundlage (Art. 6 Abs. 1 lit. f für Logs und Statistik), nicht das Widerspruchsrecht (Art. 21) und nicht das Beschwerderecht bei einer Aufsichtsbehörde (Art. 13 Abs. 2 lit. d). Zusätzlich sagt der Text „Alle Dienste laufen in EU-Rechenzentren“ und „Cloudflare (EU-Rechenzentren für EU-Besucher)“ — Cloudflare ist ein weltweites Anycast-Netz, die Zusage gilt nur mit aktivierten Regional Services, und im Repo ist davon nichts belegt. Für eine Seite, die sonst penibel auf Belegbarkeit achtet („Jede Angabe auf dieser Seite ist belegt“), ist das die auffälligste Lücke. anyplot hat sie ebenfalls — Übertrag in beide Richtungen.
- **Vorschlag:** Zwei Ergänzungen im Briefton, Fakten nüchtern: (a) neuer Absatz nach `logs`: „Rechtlich stützt sich beides — die Statistik ohne Namen und die kurz aufbewahrten Logs — auf das berechtigte Interesse, die Seite sicher und in Ordnung zu halten (Art. 6 Abs. 1 lit. f DSGVO); mehr als das wird nicht erhoben.“ (b) `rights` um Widerspruch und Beschwerderecht erweitern („Wer sich dennoch beschweren möchte, kann das bei einer Datenschutz-Aufsichtsbehörde tun, etwa der seines Wohnsitzes.“). (c) Solange Regional Services nicht bestätigt sind, `hostingIntro` abschwächen: „Cloudflare als Schutzschild ist ein weltweites Netz, das Besucher aus Europa in aller Regel über europäische Standorte bedient.“ `lastUpdated` bumpen, anyplots LegalPage gleich mitziehen. Im selben Zug die Quellenliste ehrlich machen: das Impressum nennt zwei der vier öffentlich verwendeten Vorlagen, die Schreibtafel zeigt Koch 1928 und die API führt alle vier.

### 37. [mittel] Die Messschicht: `werkzeuge.md` erklärt fünf schreibende Werkzeuge zu Nur-Lese-Werkzeugen, `dev_ids()` ist nicht reproduzierbar, und die fehleranfälligste Prozedur hat als einzige keinen Skill

- **Bereiche:** tools, skills · **Aufwand:** M · **KI jetzt** · **Owner:** Nur die Zuordnung der Taxonomie: welche Pakete als eingefrorene Lineale gelten und damit unter die Re-Baseline-Pflicht fallen (mein Entwurf liegt vor).
- **Warum:** (a) Der Rahmenabsatz von `werkzeuge.md:16` sagt „Labs, Benches und Generator schreiben nie in die DB. Einzige schreibende Gattung sind die beiden Ernte-Werkzeuge“ — tatsächlich schreiben `tools/eigenhand/{sync,setup,universe}`, `tools/lesarten/sync` und `tools/wordbench/shift_registrations` über die Admin-API in die GETEILTE Cloud-SQL-DB, und ihre `--api`-Vorgabe ist PRODUKTION. Der Doc-Körper ist an diesen Stellen korrekt; nur der Rahmen, den ein Agent zuerst liest, ist falsch. (b) `tools/inksight/prepare.py:56` gibt `tuple(TRACEBENCH_DEV_IDS)` ohne `sorted` zurück (routeg tut es richtig) — drei Prozesse liefern drei verschiedene Reihenfolgen, `frames.json` ist damit nicht byte-reproduzierbar; beide Pakete fallen zudem hinter einem stillen `except ImportError` auf 10 statt 19 Ids zurück und sagen es nirgends. (c) Die fünfstufige Tintenfolger-Mess-Liturgie protokolliert ihren eigenen Ausfall („zweimal in zwei Tagen wurde gegen den falschen Folger gemessen“) und ist als einzige stehende Prozedur ohne Skill; das BLAS-Pinning lebt nur als Prosa.
- **Vorschlag:** (a) Den Rahmenabsatz durch eine SCHREIB-MATRIX ersetzen (Werkzeug · liest woher · schreibt wohin · Freigabe-Gate) mit je einer Zeile für Labs, Benches, Ernte, Rechteck-Reparatur, Eigenhand, Lesarten, Repo-Betrieb — eine Behauptung weniger, eine Tabelle mehr. (b) `sorted()` in inksight, den stillen Fallback in beiden Paketen zu einem lauten `SystemExit`, den überholten Kommentar streichen, zwei Reihenfolge-Tests. (c) Skill `/verify-trace` mit den fünf Stufen als ausführbarer Checkliste: Fixture-Abnahme, Folger-Lauf MIT vorangestelltem `OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1`, dev-19-Scoring, `k0eval basis arm` mit der harten Regel „bricht ab bei abweichenden Stacks“, Sensoren — plus die Nachzieh-Pflicht (§14-Eintrag, Ledger-Zeile, bei Negativ die §7.9-Zeile) als Abschluss-Schritt; Zeile in beiden Anleitungs-Tabellen im selben PR. Dazu die Taxonomie-Tabelle in `werkzeuge.md` (Paket · Klasse · Änderungsregime), die heute nirgends je Paket ablesbar ist — mit dem ausdrücklichen Vermerk, dass KEIN Paket archiviert wird.

### 38. [mittel] Daten-Provenienz: die einzige Copyleft-Quelle fehlt im Index und in beiden Anleitungen, das ausgelieferte Specimen hat keine Provenienz, und LICENSE sagt maschinenlesbar „alles MIT“

- **Bereiche:** daten-lizenz · **Aufwand:** S · **KI jetzt** · **Owner:** Nur der Wortlaut des LICENSE-Geltungsbereichs — eine Rechtsformulierung am Lizenztext des eigenen Werks; Entwurf liegt vor, Freigabe durch den Autor.
- **Warum:** (a) `data/DATA_PROVENANCE.md` nennt sich „Index aller Datenartefakte“, führt aber `igerman98` nicht — ausgerechnet die einzige Quelle mit echten Pflichten (GNU GPL 2/3). Die zugehörige Autor-Regel vom 30.08. („Copyleft-Wortlisten sind Serverdaten, nie Repo-Inhalt“) steht in KEINER der beiden Agenten-Anleitungen, obwohl beide unter Commit-Klasse 2 weiterhin nur „SOURCE.md + fetch_corpus.py“ nennen. (b) Öffentlich ausgeliefert wird `offenbacher-koch-1928-excerpt.jpg`; dokumentiert ist eine 900-px-Variante, die von nichts referenziert wird — der Ableitungsschritt hat weder Origin noch Processing noch SHA256, obwohl `datenablage.md` §2 beides zur Pflicht macht. (c) LICENSE ist der unveränderte MIT-Text ohne eine Zeile über den Geltungsbereich, CITATION.cff sagt `license: MIT` — GitHubs Lizenzerkennung, SPDX-Scanner und jeder CITATION-Konsument melden damit „MIT“ für /data und für den reservierten Datensatz mit. Genau diese Trennung ist laut §5 „das ich-kenne-die-Trade-offs-Signal“ — und sie fehlt an der einen Stelle, die automatisiert gelesen wird.
- **Vorschlag:** (a) igerman98-Zeile in `data/DATA_PROVENANCE.md` unter „Korpora“ (Bytes gitignored, Serverdaten in `lesart_forms`, Migration 0028, GPL 2/3, LibreOffice-Commit); die Copyleft-Regel wortgleich als vierter Aufzählungspunkt in CLAUDE.md und copilot-instructions „Data & licensing“; bei der Gelegenheit `fetch_corpus.py` → `fetch_*.py` in beiden. (b) Zweiter Block in `data/sources/koch-1928/SOURCE.md` für die Excerpt-Datei mit Ableitung, Maßen und SHA256; die 900-px-Variante löschen oder als Reserve kennzeichnen; die Coverage-Schleife des Lizenz-Skills um `app/src/assets/specimens/` erweitern. (c) Einen kurzen Geltungsbereich-Absatz ÜBER den unveränderten MIT-Text (Code = MIT, /data je Quelle, gelernter Datensatz vorbehalten) plus `license-url` in CITATION.cff; danach `cffconvert --validate` und prüfen, dass GitHub weiterhin „MIT License“ anzeigt. Im selben PR: §5 beschreibt `compose_golden.json.gz` sachlich falsch (nicht „keine Templates“, sondern die vollen Render-Payloads von 24 Glyphen) — auf den gemessenen Sachverhalt umschreiben, ohne die Entscheidung anzutasten.

### 39. [niedrig] `apiFetch` hat keinen Timeout, und der Lint-Parkplatz aus #227 wächst statt zu schrumpfen

- **Bereiche:** app · **Aufwand:** M · **KI jetzt**
- **Warum:** (a) `app/src/lib/api/client.ts` versucht nur bei 502/503/504 oder geworfenem `TypeError` erneut; `grep AbortController|AbortSignal.timeout` über app/src → 0 Treffer. Bleibt die Antwort aus (stalled TCP, Cloud-Run-Start über 60 s, Edge hält die Verbindung), feuert weder `catch` noch Retry — `BootStatus` und `WrittenWord` zeigen den Spinner unbegrenzt und die Retry-Schaltfläche erscheint nie. Der Kaltstart-Pfad ist ansonsten sauber und live geprüft (nach ~47 s ehrlicher Fehlerzustand). (b) Issue #227 nennt 21 `set-state-in-effect`-Stellen, gemessen sind es 35 bei 69 Gesamtwarnungen (Issue: 45); ein dort gelistetes File existiert nicht mehr. Die vier `exhaustive-deps`-Stellen sind echte Kleinigkeiten (in `LetterView`/`JoinView` wird `occurrences` per `?? []` jedes Render neu gebaut und invalidiert die abhängigen `useMemo`s).
- **Vorschlag:** (a) Pro Versuch `signal: AbortSignal.timeout(ms)` in `apiFetch` (Default ~20 s Boot-Reads, ~30 s `/write/word`, über `RetryOptions` überschreibbar); ein Abbruch zählt wie ein Netzfehler. Test mit nie auflösendem `fetch`-Mock plus `vi.useFakeTimers()`. Im selben Zug `RouteError.tsx` um anyplots Chunk-Erkennung mit sessionStorage-Sperre und den 404-Zweig erweitern — zusammen mit dem `Cache-Control`-Fix aus Rang 6 schließt das genau den Fehler, der nach einem Deploy auftritt. (b) Issue #227 auf den Ist-Stand heben, die vier `exhaustive-deps`-Stellen sofort fixen, eine CI-Ratsche einziehen (`eslint -f json` zählen, Obergrenze 69 darf nur sinken) und Tranche 1 auf den ÖFFENTLICHEN Animationsflächen abräumen (WrittenGlyph/WrittenWord/WrittenSheet/QuestionVisual/QuizResultsPanel/ScribeView/useElementSize) — disjunkt zur Werkbank.

## Parallelplan (Tracks, die eine KI-Session gleichzeitig starten kann)

### Track 1: T1 — Laufform LF11 „Glatte Zeile“ (das Geschriebene, trockenes Experiment)

- **Scope:** Den Zeilen-Builder vom unabhängigen Per-Anker-Median auf einen glatten Schätzer heben: je Vorkommen den Fit pro Zug auf eine B-Spline mit Knoten alle ~0,08 xh projizieren (Ecken und Strichanfänge aus `trace_meta` als feste Knoten), Median über die Kontrollpunkte, zurück auf 120 Anker. Trocken bauen, Kandidaten-Karte messen, Gates a–e wie LF3b-W plus einen Glätte-Sensor (Zickzacks/xh ≤ Chart + 1). Arm-Nummer: LF11, weil LF10 durch PR #474 belegt ist — die Kollision im selben PR benennen. Schritt 0 jeder Sprosse: `uv sync --all-extras` + `fetch_fixtures --set all --verify` und die §15-Kopfzahl reproduzieren. KEIN Prod-Write in dieser Sitzung.
- **Dateien:** `core/aggregate.py`, `core/laufform.py`, `tools/laufform/harvest.py`, `tools/laufform/inventory.py`, `tests/test_aggregate*.py`, `docs/reference/qualitaetsmetrik.md (§14, neuer Abschnitt am Dateiende)`
- **Verifikation:** /optimize-glyphs (Frozen-Bench-Disziplin) + /verify-core · **Aufwand:** L · **Abhängigkeit:** Textueller Konflikt mit T3 und T5 auf docs/reference/qualitaetsmetrik.md — jeder Track hängt seinen eigenen §14-Abschnitt ans Dateiende an, nie mitten hinein. Der Prod-Write der neuen Zeilen hängt an T4 (humanbench-Urteil) und am Autor-Go.
- **Owner-Go nötig:** nein

### Track 2: T2 — Wordbench-Sensorik: Naht-Winkel und Wurzel-Identität

- **Scope:** Zwei Sensoren in den Wordbench, beide byte-identisch in der Kopfzahl: (a) `root_digest(root)` — SHA-256 über die sortierte Liste (Pfad, Größe, SHA-256) der Fixture-Wurzel — plus zwei Kopfzeilen je Lauf (`root: <name> exported_at=… digest=…`), ein `--expect-root <digest>`, das bei Abweichung abbricht, und die Prüfung des vorhandenen `page_sha256` im Lauf statt nur beim Neuaufbau; (b) Report-Spalte `seam_deg` (Abgang/Ankunft-Δ je Join). Damit sind die Messgrundlage und der Naht-Knick zum ersten Mal Zahlen statt Behauptungen.
- **Dateien:** `tools/wordbench/run.py`, `tools/wordbench/summary.py`, `tests/test_wordbench_*.py`
- **Verifikation:** /verify-core (+ ein Bench-Lauf vorher/nachher: Kopfzahl muss byte-gleich bleiben) · **Aufwand:** S · **Abhängigkeit:** Keine. Voraussetzung für T3 (der Arm braucht seam_deg) und liefert die Zahl, die T5 als datierten Nachtrag festhält.
- **Owner-Go nötig:** nein

### Track 3: T3 — Austritts-Kollinearität (Klassenregel, das Geschriebene)

- **Scope:** Für Sägezahn-Austritte im ALIGN_TAN_DEG-Band unter HIGH_COUPLE_EXIT_Y den A-Stub bis zur Fußwende zurücktrimmen (Spiegel von `entry_trim`, auf Centerline UND Silhouette) und die Gerade von dort durch den bestehenden Kopplungspunkt legen; Platzierung unangetastet. Vorregistrieren mit Gates: word/pair ≤ +0,002 · `dconn` fällt in ≥ 60 % der Klassen-Joins · `doff` byte-gleich · Kompositions-Soll ohne Verlust · seam_deg-Median der Klasse < 5°. Kill: dconn steigt → verworfen, Rettungsweg „nur die Ankunftsseite“ als eigener Arm. Die Adoption bricht das Golden — bis zum Go endet der Track bei der gemessenen Zahl.
- **Dateien:** `core/compose.py`, `tests/test_compose_joins.py`, `tests/test_compose_golden.py`, `docs/reference/qualitaetsmetrik.md (§14, eigener Abschnitt)`
- **Verifikation:** /verify-core + /optimize-glyphs · **Aufwand:** M · **Abhängigkeit:** T2 (seam_deg-Sensor). §14-Textkonflikt mit T1/T5 wie oben. Berührt core/compose.py, das sonst kein Track anfasst.
- **Owner-Go nötig:** Nur für die Adoption: „Darf der Golden-Fixture für den Austritts-Trim neu gebacken werden (deklarierte Re-Baseline)?“ — Messung und Verdikt gehen ohne.

### Track 4: T4 — humanbench-Wortmodus mit der Echtheitsfrage (das Instrument)

- **Scope:** Wortmodus für den Urteilsdurchgang bauen: ein Specimen-Crop, zwei Kompositionen (Basis/Kandidat) als Overlays, die Frage „welche sieht echter geschrieben aus?“ per Schalter in `page.py::CHOICES`, Seitenzufall aus der Saat, 12 gespiegelte Wiederholungen nach Verdachtskategorie geschichtet, Auswerteplan VORHER festgeschrieben (Adoption ≥ 60 % Kandidat, ≤ 25 % „kein Unterschied“), Stempel nach dem Muster der beiden archivierten Runden. Erste Runden vorbereiten für LF11 (T1) und den Platten-Nib. Das ist das einzige Instrument, das Zickzack, Strichbreite und Naht-Knick sieht.
- **Dateien:** `tools/humanbench/build.py`, `tools/humanbench/page.py`, `tools/humanbench/analyze.py`, `tests/test_humanbench*.py`, `docs/reference/menschliche-bewertung.md`
- **Verifikation:** /verify-core · **Aufwand:** M · **Abhängigkeit:** Keine für den Bau. Die ERSTEN Runden brauchen die Kandidaten aus T1 bzw. den Nib-Schalter — bis dahin mit einem synthetischen Kandidatenpaar abgenommen.
- **Owner-Go nötig:** nein

### Track 5: T5 — Journal-, Verfahrens- und Kampagnen-Register (die Kampagne wieder lesbar machen)

- **Scope:** Registertabelle als erster Block von §14 (74 Zeilen: Datum · Route · Arm · Typ · Verdikt · Anker; die CSV liegt im Scratchpad) plus Headline-Ledger; Verfahrensseiten-Sync aug20→sep01 (Übersicht auf Kette v5, Lotse-Ledger um L-U und LF3b-W, InkSight „T0, auf Lineal 1,5 unvermessen“, Nullprobe-Zeile, Kette-Tabelle reparieren, index.md-Kommentare); datierter Nachtrag zur undeklarierten aug30-Re-Baseline; „Stand der Kampagne“-Blockquote und §7.11 „Offene Arme“ in tintenfolger.md; die vier fehlenden §7.9-Zeilen; Statuswörter ins Vokabular; Glossar-Hygiene (Sprung-Gate, W1–W6, Klassenregel-Liste gegen die 14 `*_BASES`) und die fünf fehlenden Mechanismus-Einträge; eigenhand-erfassung.md in den Strukturbaum; werkzeuge.md um routeg und inkpilot. Als Wiederholungsschutz `tools/docs_register check` neben dem Changelog-Gate.
- **Dateien:** `docs/reference/qualitaetsmetrik.md`, `docs/reference/verfahren.md`, `docs/reference/verfahren-{kette,lotse,inksight,nullprobe}.md`, `docs/proposals/tintenfolger.md`, `docs/reference/glossar.md`, `docs/reference/werkzeuge.md`, `docs/index.md`, `docs/contributing.md`, `tools/docs_register/`
- **Verifikation:** /write-docs · **Aufwand:** L · **Abhängigkeit:** Textueller §14-Konflikt mit T1 und T3 (jeder Track hängt seinen Abschnitt ans Dateiende; das Register steht am Anfang der Sektion und wird von T1/T3 nur um je eine Zeile ergänzt). Der Nachtrag zur aug30-Wurzel wird durch T2s Digest-Ausgabe künftig überflüssig.
- **Owner-Go nötig:** Aus welcher Sitzung / welchem Export die Wurzel hinter 0,106400 / 0,146580 stammt — ohne Antwort wird sie als „undeklarierte Re-Baseline, Zahlen ab aug30 nur untereinander vergleichbar“ markiert.

### Track 6: T6 — Die zwei toten Werkzeuge: Lesart-Ehrlichkeit und Lesetafel-PDF

- **Scope:** (a) `/lesen/vergleichen` hört auf zu lügen: die Leerzustände exklusiv machen (`dictionary ? t.noLesarten : t.dictionaryMissing`), die separate Wörterbuch-Zeile nur bei vorhandenem Wörterbuch, neuer Locale-Key im Briefton, Vitest über alle drei Zustände; Lesen-Hub und llms.txt entschärfen, solange kein Bestand geladen ist. (b) `crossOrigin="anonymous"` an das Anzeigebild in TafelView, Kommentar in useLesetafelPdf, Vitest der das Attribut pinnt, Live-Gegenprobe ohne Scrollen. (c) Im selben Rahmen: CLS-Maße an die drei Grundtafel-Scans, Antiqua-Beschriftung je Zelle der geschriebenen Tafel, exampleWord-Rückfallliste, Quiz-Auswertung auf `/tafel?g=<key>` verlinken.
- **Dateien:** `app/src/sections/vergleichen/VergleichenView.tsx`, `app/src/locales/de/vergleichen.ts`, `app/src/sections/tafel/`, `app/src/sections/quiz/QuizResultsPanel.tsx`, `app/src/locales/de/tafel.ts`, `app/src/locales/de/hub.ts`, `app/public/llms.txt`
- **Verifikation:** /verify-frontend · **Aufwand:** M · **Abhängigkeit:** locales/de/hub.ts wird auch von T10 (Inhalt/SEO) angefasst — Absprache: T6 ändert nur den Lesart-Satz, T10 die Hub-Absätze. Der tatsächliche Wörterbuch-Bestand hängt am Autor-Ladelauf, der Code-Fix nicht.
- **Owner-Go nötig:** nein

### Track 7: T7 — Übungsblatt-Robustheit

- **Scope:** Ohne Doktrin-Entscheid sofort: `max` auf den drei Verhältnis-Feldern und Wertklemmung; Leerblatt-Warnung statt stiller leerer Vorschau; PDF-Knopf sperren, wenn nichts zu drucken ist; sichtbare Zeile bei Schrift-Mismatch („Die Vorschrift wird in Sütterlin gesetzt — Kurrent ist noch nicht nachgeschrieben“) plus derselbe Zusatz in der gedruckten Fußzeile; Hilfetext auf die tatsächlich mögliche Zeichenzahl umstellen; Label-Umbruch bei 360 px reparieren. Nach dem Doktrin-Entscheid: Umbruch an der Wortgrenze bzw. Skalierung und eine Rückmeldung mit Zeilennummer unter dem Textfeld. Regressionstest: eine 37-Zeichen-Zeile bei 6 mm liefert nie ein leeres Ergebnis.
- **Dateien:** `app/src/sections/worksheet/`, `app/src/locales/de/worksheet.ts`, `app/src/sections/worksheet/useWorksheetText.test.ts`
- **Verifikation:** /verify-frontend · **Aufwand:** M · **Abhängigkeit:** Keine — einzige Fläche, die kein anderer Track berührt.
- **Owner-Go nötig:** Darf eine zu breite Vorschrift-Zeile herunterskaliert oder an einer Wortgrenze umgebrochen werden (dann stimmt ihre Höhe nicht mehr exakt zur Lineatur), oder bleibt die Lineatur unantastbar und die Seite weist nur sichtbar hin?

### Track 8: T8 — Öffentliche Bedienbarkeit: Fokus, Kontrast, Typo-Boden, Touch

- **Scope:** Eine Theme-Runde statt zwölf Einzelfixes: `MuiButtonBase.Mui-focusVisible` mit viridianem 2-px-Ring; `MuiChip.labelSmall`/`MuiButton.sizeSmall` auf 0,875rem; `MuiLink` auf `always` + viridianText; Landing-CTA auf 600 (oder viridianText); `specimenCaptionSx` und die drei Landing-Marken auf `variant="caption"`; gemeinsame `hitArea`-sx-Hilfe für ReplayButton, InfoHint, „beenden“, „Einstellungen ändern“ und die Umschaltgruppen; `env(safe-area-inset-*)` im Seiten-Innenmaß; Zurück-nach-oben für die langen Inhaltsseiten; 404-Seite bekommt ihre Fußzeile und ein `page_not_found`-Ereignis; RouteError um Chunk-Erkennung mit sessionStorage-Sperre. Abschluss: Tastatur-Durchgang über vier Routen mit Vorher/Nachher, Messskript `app/scripts/type-floor.mjs` einchecken, design-system.md §9 um Fokus-, Link- und Touch-Regel ergänzen.
- **Dateien:** `app/src/theme/`, `app/src/styles/paper.ts`, `app/src/components/`, `app/src/pages/NotFoundPage.tsx`, `app/src/routes/RouteError.tsx`, `app/scripts/type-floor.mjs`, `docs/concepts/design-system.md`
- **Verifikation:** /verify-frontend · **Aufwand:** M · **Abhängigkeit:** Theme-Ebene: die Fixes in T6/T7 erben davon, kollidieren aber nicht (andere Dateien). design-system.md wird sonst von keinem Track angefasst.
- **Owner-Go nötig:** Zwei Geschmacksfragen: durchgehende Unterstreichung im Fließtext oder nur Farbwechsel; und ob die 44-px-Touch-Regel bindend in design-system.md §9 aufgenommen wird.

### Track 9: T9 — Auslieferung und Image härten

- **Scope:** `app/security-headers.conf` aus anyplot übernehmen und die CSP auf die tatsächlichen Quellen dieser Seite schreiben; im Server-Block UND in jedem `location` mit eigenem `add_header` per `include` ziehen; `location = /index.html { add_header Cache-Control "no-cache"; }` vor `location /` mit Begründungskommentar; im Dockerfile mitkopieren. Dazu `npm ci` ohne `|| npm install`, ein `app/.dockerignore`, `.env*` in `.gcloudignore`. Auf der API-Seite: `UV_COMPILE_BYTECODE=1` plus `compileall` im Runtime-Stage, uv-Version und `UV_PYTHON` gepinnt, dann `-X importtime` im Container gegen die alte Zahl messen und die Kaltstart-Zeile in cloudbuild.yaml ehrlich machen. Schlanke Starlette-Middleware mit `nosniff` + `Referrer-Policy` für den API-Host.
- **Dateien:** `app/nginx.conf`, `app/security-headers.conf (neu)`, `app/Dockerfile`, `app/.dockerignore (neu)`, `api/Dockerfile`, `api/main.py`, `.gcloudignore`
- **Verifikation:** /verify-frontend (CSP-Durchgang durch Quiz, Federprobe, Tafel und die Werkbank) + /verify-api · **Aufwand:** M · **Abhängigkeit:** Berührt api/cloudbuild.yaml NICHT (das gehört T12) und app/src NICHT. `app/nginx.conf` ist laut Crawler-Richtlinie an anyplots Datei gekoppelt — im selben Atemzug prüfen.
- **Owner-Go nötig:** CSP eine Woche als Report-Only oder direkt scharf ausliefern (ein Fehler dort macht die Werkbank unbenutzbar), und HSTS 180 Tage ohne includeSubDomains/preload bestätigen.

### Track 10: T10 — Inhalt und SEO-Ehrlichkeit

- **Scope:** Quiz-Prerender auf `offersChoice` filtern und den Drift-Guard um „keine unavailable-Option“ erweitern; „erklärt jeden Fehlgriff“ auf die belegbare Zusage; 1941 Erlass/Rundschreiben nach dem Faktenblatt korrigieren (plus Holzschliff-Datierung und die Sütterlin-Nuance); fünf Meta-Descriptions unter 155 Zeichen und das Test-Gate von 200 auf 155 senken; erklärende Absätze für /quiz und /federprobe, Substanz für /lesen und /schreiben; Terminologie-Sweep (langes ſ/rundes s, „die Feder“, eine Schreibweise je Quelle, gespacte Verhältnisse, llms.txt-Anführungszeichen und „Lese-Quiz“); Impressum-Quellenliste auf alle vier Vorlagen; sitemap-lastmods setzen und einen Wächter im Prerender-Build, der sie gegen `git log` der beteiligten Locale-Dateien hält; Glossar-Einträge für die Produktnamen (Schreibtafel, Lesetafel, Grundtafel, Federprobe, Lesart prüfen, „Zug um Zug“).
- **Dateien:** `app/src/locales/de/{seo,hub,quiz,scribe,schriftkunde,landing,impressum}.ts`, `app/src/lib/seo/prerender.ts`, `app/src/routes/seoCoverage.test.ts`, `app/prerender/`, `app/public/sitemap.xml`, `app/public/llms.txt`, `docs/reference/glossar.md`
- **Verifikation:** /verify-frontend + /write-docs · **Aufwand:** L · **Abhängigkeit:** locales/de/hub.ts auch in T6 (dort nur der Lesart-Satz); docs/reference/glossar.md auch in T5 (dort die Mechanismus-Einträge in §2/§3, hier ein neuer Block „Öffentliche Seiten“ — verschiedene Abschnitte). Der Datenschutz-Teil bleibt draußen, bis der Autor den Text freigibt.
- **Owner-Go nötig:** Darf ein „So geht es“-Dreischritt zwischen Hero und „Drei Schriften“, und darf der Lead die Familienforschung ausdrücklich nennen? Beides ändert die Landing-Struktur, die seit dem Redesign steht.

### Track 11: T11 — Werkbank-Politur (die Fläche, an der nur der Autor arbeitet)

- **Scope:** Dirty-Guard gegen Escape, Backdrop-Klick und „Schließen“ im Wizard samt Bestätigungsdialog und sessionStorage-Entwurf; Sperrzustand vor der Arbeit zeigen und die Wizard-Alerts nach Severity trennen; deutsche Fehlerschicht `apiFehlertext(err)` mit englischem Rohdetail im `<details>` über alle 18 Stellen; `no-occurrences` neben `no-hand`; „fehlend:“-Chip in JoinView und WordView aus `onResolved`; ruhiger Präsens-Satz statt des falschen Erfolgsalarms; `scrollIntoView` beim Aufklappen der Tafel; a11y-Durchgang (h1 über `titleText`, aria-label an neun Slider und zwei Zoom-Knöpfe, Kontext an die „Öffnen“-Knöpfe, `aria-expanded` am Korb); Eigenhand-Feinschliff (`semicolon`/`dash` in KEY_GLYPHS mit Drift-Test, doppelte Quoten-Caption, Terminal-Befehle in Monospace mit Kopieren-Knopf); der 1-px-Überlauf in Schritt 4; das Korb-Protokoll-Loch in `work_items.py:117` samt drei Testzeilen.
- **Dateien:** `app/src/sections/admin/`, `app/src/locales/de/{admin,wizard}.ts`, `app/src/lib/api/`, `api/routers/work_items.py`, `tests/test_api_work_items.py`, `docs/proposals/optimierungs-werkbank.md (§5.1)`
- **Verifikation:** /verify-frontend + /verify-api · **Aufwand:** L · **Abhängigkeit:** `app/src/lib/api/` wird auch von T13 (apiFetch-Timeout) angefasst — verschiedene Dateien im selben Verzeichnis, Absprache: T11 legt `apiFehlertext`, T13 fasst `client.ts` an. api/routers/work_items.py berührt kein anderer Track.
- **Owner-Go nötig:** Sperr-Doktrin: soll der Wizard eine gesperrte Glyphe gar nicht erst zum Zeichnen anbieten, oder mit sichtbarem Schloss plus bewusstem „Trotzdem überschreiben (force)“? Und: heißt der Abzug künftig „Deckungslücke“, oder zeigt die Oberfläche überall den positiven Wert?

### Track 12: T12 — Repo-Vorzeige und CI-Wächter

- **Scope:** Bot-Serving-Wächter driftfest machen (checkout, Titel aus `app/prerender/*.html` ableiten, alle 11 Seiten) und einen Alarmpfad geben (`issues: write`, Issue bei failure, Schließen bei success); Image-Job in ci.yml (build ohne push + Container-Smoke mit Versionsvergleich und COPY-Pin + hadolint); `timeout-minutes` je Job; acht Actions auf Commit-SHAs; `push-latest` hinter `promote` in beiden cloudbuild-Dateien; App-Deploy auf die Kandidaten-Kette mit Bot-Pfad-Smoke; README-Schlüsseltupel korrigieren; CODE_OF_CONDUCT.md und `.github/pull_request_template.md`; ehrliche Frontend-Coverage (`test.coverage`-Block in vite.config.ts + codecov.yml); `app/package.json`-description, pyproject-classifiers und -urls; die vier Dependabot-PRs per `@dependabot rebase` entblocken und grün melden.
- **Dateien:** `.github/workflows/bot-serving-check.yml`, `.github/workflows/ci.yml`, `.github/pull_request_template.md (neu)`, `CODE_OF_CONDUCT.md (neu)`, `README.md`, `codecov.yml`, `app/vite.config.ts`, `app/package.json`, `pyproject.toml`, `api/cloudbuild.yaml`, `app/cloudbuild.yaml`
- **Verifikation:** /open-pr (die Gate-Batterie selbst ist der Prüfstein) · **Aufwand:** L · **Abhängigkeit:** Berührt Dockerfiles NICHT (die gehören T9). `app/package.json` auch in T13 (nvmrc/plugin) — Absprache: T12 nur description, T13 die devDependencies. `uv.lock` bleibt T14 vorbehalten.
- **Owner-Go nötig:** Badge-Form (eine ehrliche Gesamtzahl ~59 % oder zwei Flag-Badges) und die Repo-Einstellungen: Required Check „Migrations“, `delete_branch_on_merge`, Squash-only, die fünf Sicherheitsschalter.

### Track 13: T13 — API-Hygiene, Zulieferkette und Client-Robustheit

- **Scope:** 4-Dezimal-Walk in `_geometry_response` plus dieselbe Rundung im `--verify`-Vergleich von `fetch_fixtures` (sonst bricht das Bit-Gate); In-Process-Token-Bucket für `/write/word*` mit 429 und `Retry-After`; Gate-Pin auf alle 33 Nicht-GET-Operationen in `test_api_public_surface.py` mit ausdrücklich leerer Ausnahmeliste; fünf veraltete Starlette-Konstanten plus modulunabhängiger Warnfilter plus 413-Test; `version` in `/health` und Smoke-Assert; HeadAsGetMiddleware von anyplot; Smoke-Key `n-medial` → `n`; `rendering.py`-Docstring auf Mehr-Instanz-Semantik; die vier verwundbaren Laufzeitpakete heben und `pip-audit`/`npm audit` als CI-Job; `AbortSignal.timeout` in `apiFetch`; `.nvmrc` 22 + `engine-strict`.
- **Dateien:** `api/routers/write.py`, `api/routers/templates.py`, `api/routers/eigenhand.py`, `api/routers/health.py`, `api/main.py`, `api/rendering.py`, `api/request_context.py`, `tests/test_api_public_surface.py`, `tests/test_api_http.py`, `tools/wordbench/fetch_fixtures.py`, `app/src/lib/api/client.ts`, `uv.lock`, `pyproject.toml`, `app/.nvmrc (neu)`
- **Verifikation:** /verify-api + /verify-core · **Aufwand:** L · **Abhängigkeit:** `tools/wordbench/fetch_fixtures.py` ist bewusst hier und nicht in T2 (dort nur run.py). `pyproject.toml` auch in T12 (Metadaten) — Absprache: T12 classifiers/urls, T13 filterwarnings und Abhängigkeiten. `app/src/lib/api/client.ts` gegen T11 abgegrenzt.
- **Owner-Go nötig:** nein

### Track 14: T14 — Skills, Agent-Anleitungen und Daten-Provenienz

- **Scope:** Die vier unbrauchbaren Skill-Stellen reparieren (verify-api-Sweep, pgserver-Weg in verify-migrations samt .env-Trap und Snapshot-Hinweis, optimize-glyphs Zeile 109 plus Re-Export-Verbot, start.md ohne `alembic upgrade head` gegen die geteilte DB); open-pr-Runde (alembic-Zeile in die Gate-Tabelle, `origin/main...`, lint+test in die lokalen Gates, §4 Deploy-Watch, `Fixes #N`, „commit/push“ im Trigger); verify-frontend an design-system.md binden und einen „was CI prüft“-Abschnitt geben; audit-licenses base64-Netz auf ein Klassenmuster umstellen und die Inhalts-Pickaxe fürs History-Netz ergänzen; neue Skills `/verify-trace`, `/dbsnapshot`, `/release`, `/dependabot`. Anleitungen: die fünf Copilot-only-Regeln und die vier Memory-Direktiven in beide Dateien, Pre-commit-Ist-Stand, Listen → Zeiger, prime.md auf den Run-Block, `tests/test_agent_instructions.py`. Daten: igerman98 im Index, Copyleft-Regel in beide Anleitungen, Specimen-Provenienz, SOURCE.md-Hash-Lücken, LICENSE-Geltungsbereich, §5-Korrektur zum compose-Golden, style-guide-Fonttabelle, das NUL-Byte in payloads.ts.
- **Dateien:** `.claude/skills/`, `.claude/commands/{prime,start}.md`, `CLAUDE.md`, `.github/copilot-instructions.md`, `tests/test_agent_instructions.py (neu)`, `data/DATA_PROVENANCE.md`, `data/sources/*/SOURCE.md`, `LICENSE`, `CITATION.cff`, `app/THIRD_PARTY_NOTICES.md`, `docs/concepts/style-guide.md`, `app/src/components/SpecimenStrip/payloads.ts`
- **Verifikation:** /write-docs (+ /audit-licenses für den Daten-Teil) · **Aufwand:** L · **Abhängigkeit:** Berührt docs/reference/* NICHT (das gehört T5) und app/src nur mit einer Zeichen-Änderung in payloads.ts. Der LICENSE-Absatz wartet auf die Freigabe des Wortlauts; alles andere läuft ohne.
- **Owner-Go nötig:** Wortlaut des LICENSE-Geltungsbereichs (Entwurf liegt vor) und der Umgang mit dem reservierten Blob in der Historie — bis dahin baut der Track nur das Nachweis-Netz.

## Fragen an den Autor

### F1. Soll `tools.lesarten.sync` gegen api.kurrentschrift.ink laufen, damit /lesen/vergleichen überhaupt Lesarten anbieten kann?

- **Warum:** Das Wörterbuch ist in Produktion leer (live nachgeprüft: `dictionary: null` für jede Abfrage), und die Seite behauptet trotzdem „sie ist wohl eindeutig“. Der Code-Fix (die Zustände exklusiv machen) läuft unabhängig davon, aber ohne Bestand bleibt das Kernwerkzeug für Genealogen leer — während Lesen-Hub und llms.txt es weiter versprechen. Der Ladelauf ist ein prod-berührender Schreibvorgang über die Admin-API (≈720.000 Formen, generationsweise + commit).
- **Optionen:** Jetzt laden (ADMIN_TOKEN, Freigabe in derselben Sitzung, dbsnapshot vorher) · Später laden und bis dahin die Zusage auf Hub und in llms.txt zurücknehmen · Gar nicht laden und den Abschnitt „Wörter, die genauso aussehen könnten“ ausbauen
- **Empfehlung:** Jetzt laden. Der Code-Fix macht die Seite ehrlich, aber ehrlich-und-leer ist für das Werkzeug, das Genealogen suchen, kein Zustand, den man auf einer Vorzeige-Seite stehen lässt. Wenn heute keine Zeit ist: Fix mergen, Zusage zurücknehmen, Ladelauf als Todoist-Aufgabe.

### F2. Fünf GitHub-Sicherheitsschalter, `delete_branch_on_merge`, Squash-only und der Required Check „Migrations“ — dürfen die acht Repo-Einstellungen in einem Zug umgelegt werden?

- **Warum:** Alle acht sind Einstellungen, die nur du ändern kannst, alle acht sind kostenlos, und im Schwesterrepo laufen die sicherheitsrelevanten bereits. Zusammen erklären sie drei Befunde: warum 26 Advisories niemandem auffallen, warum 121 Branch-Leichen neben dem README liegen, und warum ein roter Migrationslauf den Merge nicht blockiert, aber den Deploy killt.
- **Optionen:** Alle acht in einer Sitzung (≈10 Minuten in den Settings) · Nur die fünf Sicherheitsschalter jetzt, der Rest später · Einzeln nach Rückfrage
- **Empfehlung:** Alle acht. Sie sind voneinander unabhängig, keiner ist riskant, und beim Einschalten von Secret Scanning ist mit historischen Treffern zu rechnen — die sind eine Information, kein Schaden. Vor der Branch-Löschung die Liste ausgeben lassen und gegen `git worktree list` filtern (vier Worktrees, darunter der von PR #474).

### F3. Sicherheits-Header und CSP: eine Woche als Report-Only fahren oder direkt scharf ausliefern? Und gilt HSTS mit 180 Tagen ohne Preload wie in anyplot?

- **Warum:** Die Seite liefert heute keinen einzigen Sicherheits-Header aus (live nachgeprüft), während anyplot seit seinem Audit einen vollständigen, kommentierten Satz fährt. Eine zu enge CSP macht die Werkbank unbenutzbar — MUI/Emotion-Inline-Styles, Blob-PDF-Download, der Plausible-Proxy —, und das merkt man erst beim Arbeiten, nicht beim Deploy.
- **Optionen:** Eine Woche Report-Only, Verstöße sammeln, dann scharf · Direkt scharf mit den erhobenen Quellen (die CSP-Quellen sind aus index.html und app/src verifiziert) · Nur die vier billigen Header (nosniff, X-Frame-Options, Referrer-Policy, HSTS) jetzt, CSP später
- **Empfehlung:** Eine Woche Report-Only für die CSP, die vier übrigen Header sofort scharf. HSTS mit 180 Tagen ohne includeSubDomains/preload — genau anyplots konservativer Erstrollout; Preload ist eine Einbahnstraße und lohnt erst, wenn alle Subdomains sicher sind.

### F4. Übungsblatt: darf eine zu breite Vorschrift-Zeile herunterskaliert oder an einer Wortgrenze umgebrochen werden, um zu passen — oder bleibt die Lineatur unantastbar und die Seite weist nur hin?

- **Warum:** Ein ganz gewöhnlicher Satz erzeugt heute ein leeres A4-Blatt ohne jeden Hinweis, und der PDF-Knopf bleibt aktiv. Die Frage entscheidet den Umfang: Umbruch/Skalierung heißt, die Vorschrift-Höhe stimmt nicht mehr exakt zur gewählten Mittellänge — der Nachspur-Zweck bleibt, die Maßtreue nicht.
- **Optionen:** Umbruch an der Wortgrenze (Vorschrift-Höhe bleibt, Zeilenzahl wächst) · Skalierung der Zeile auf die verfügbare Breite (eine Zeile, kleinere Schrift) · Lineatur unantastbar: nur sichtbarer Hinweis mit Zeilennummer, PDF-Knopf gesperrt
- **Empfehlung:** Umbruch an der Wortgrenze plus sichtbarer Hinweis. Er hält die Maßtreue der Lineatur, ist für den Nachspur-Zweck unschädlich, und der Hinweis ist ohnehin fällig — die stille Variante ist die einzige, die nicht bleiben darf.

### F5. Soll die öffentliche Schrift den aus den Wortproben gemessenen Platten-Nib (≈0,19 xh) statt des chart-gepoolten (0,145 xh) schreiben?

- **Warum:** Der gerenderte Strich ist über 16 Wörter gemessen rund 25 % dünner als die Platte — der zweitgrößte Eindrucksunterschied nach dem Zickzack und in jeder Montage sichtbar. Kein Lineal trägt Strichbreite, die Entscheidung ist also nicht messbar zu erzwingen; sie ist eine Aussage über das Erscheinungsbild.
- **Optionen:** Nach einer humanbench-Paarrunde entscheiden (≥ 60 % „echter“ = adoptieren) · Sofort umstellen, der Messwert der Platte ist die Referenz · Chart-Nib bewusst behalten (dünner = leichter lesbar am Bildschirm)
- **Empfehlung:** Nach der Paarrunde. Der Schalter kostet fast nichts und bleibt bis dahin aus; ohne das Instrument wäre die Umstellung genau die Sorte Geschmacksentscheidung, die die Pre-Reg-Doktrin dieses Projekts sonst überall vermeidet.

### F6. Tafelformen und d-Auslauf: fährst du t, ſ, k, d und r im Wizard nach — oder darf der Anstrich per Klassenregel grammatisch absorbiert werden? Und gilt für den d-Auslauf (a) Variantentrennung oder (b) Übergangsgenerator?

- **Warum:** Drei Chart-Formen widersprechen messbar der Platte (konkaver Anstrich mit 0,14–0,19 xh Bogen, d-Schleife 1,10 statt ≈0,66 xh, r-Fahne als Balken), und der d→a-Verbinder fällt aus der Schleifenkreuzung bei y 1,17 mit −34° ins a, während die reale Tinte bei y 0,96 abgeht und steigt. Die Doktrin weist die Tafel dem Menschen zu; die d-Auslauf-Frage steht seit dem 26.08. offen und blockiert eine der sichtbarsten Stellen in „das“, „die“, „und“.
- **Optionen:** Alles im Wizard (Doktrin-rein, kostet drei Nachfahr-Sitzungen) · Formen (d-Schleife, r-Fahne) im Wizard, Anstrich per Klassenregel absorbieren · d-Auslauf (a) Variantentrennung verbunden/terminal · d-Auslauf (b) Auslauf im Übergangsgenerator
- **Empfehlung:** Formen im Wizard, Anstrich per Grammatik — der konkave Kopf ist ein Übergangsproblem, das die J-Serie ohnehin schon dreimal von der falschen Seite angegangen ist; die Schleifenbreite und die Fahne sind echte Formfragen und gehören dir. Für den Auslauf (b): die Empfehlung aus §12 steht seit August, und (a) verdoppelt die Bibliothekseinheit für einen Effekt, den der Generator ohnehin erzeugen muss.

### F7. Wann terminierst du die humanbench-Urteilssitzung (≈10 Minuten je Kandidat, ~63 Bildschirme)?

- **Warum:** Sie ist das einzige Instrument, das die drei sichtbarsten Defekte des Geschriebenen sieht, und sie hängt an drei Stellen als Tie-Breaker: K-E1/K-E2, der Restpreis der 13 v5-Rückweisungen und die Methodik-Lücke in §7.9. Solange sie nicht gefahren ist, bleiben LF11, der Platten-Nib und der Austritts-Trim gemessen-aber-unentscheidbar — und das ist die eigentliche Bremse am erklärten Hauptziel.
- **Optionen:** Erst das Instrument bauen lassen, dann eine Sitzung mit zwei Kandidaten (LF11 + Nib) · Instrument bauen und die erste Runde mit einem synthetischen Kandidatenpaar abnehmen, echte Runden später · Zurückstellen, bis mehr Kandidaten anstehen
- **Empfehlung:** Instrument bauen lassen und eine Doppelsitzung ansetzen, sobald LF11 seine Karte hat. Zehn Minuten deiner Zeit lösen drei seit Wochen hängende Entscheidungen — das ist der beste Stundenlohn in dieser ganzen Liste.

### F8. Der Codecov-Badge zeigt 87 %, ehrlich sind ~59 % — eine ehrliche Gesamtzahl oder zwei Flag-Badges?

- **Warum:** Die Frontend-Hälfte wird heute nur über die von Tests importierten Module gemessen (18,1 % über die ganze SPA-Quelle statt 82,4 %). Für ein Repo, dessen Zweck „Vorzeige“ ist, steht damit eine nachprüfbar falsche Zahl an der prominentesten Stelle. Die Messung zurückzudrehen wäre die einzige Option, die nicht in Frage kommt.
- **Optionen:** Eine ehrliche Gesamtzahl (~59 %) · Zwei Flag-Badges: backend 88 % / frontend 18 % · Kein Coverage-Badge im README
- **Empfehlung:** Zwei Flag-Badges. Sie sind ehrlich UND informativ: 88 % Backend ist ein starkes Signal, das in einer Mischzahl untergeht, und 18 % Frontend benennt genau die Lücke, die die fehlenden Komponententests sind — das liest sich als Selbstkenntnis, nicht als Schwäche.

### F9. Der reservierte Datensatz in der öffentlichen Git-Historie: purgen oder dokumentiert annehmen?

- **Warum:** `.design-sync/previews/_writtenGlyphData.ts` (32 KB, Diagnose-Payloads zweier Templates) liegt seit dem 20.06. in der öffentlichen Historie und wurde am 31.07. nur aus HEAD entfernt. Ein Purge bedeutet einen History-Rewrite plus Force-Push auf einen öffentlichen main und macht bestehende Klone und Forks nicht sauber — er senkt die Auffindbarkeit, nicht die Verbreitung.
- **Optionen:** Annehmen und in quellen-und-rechte.md §5 als bekannte, akzeptierte Ausnahme dokumentieren · Purgen (git filter-repo, Force-Push, GitHub-Support für den gecachten Blob) · Erst prüfen, ob weitere solche Blobs existieren, dann entscheiden
- **Empfehlung:** Annehmen und dokumentieren — 2 von ~80 Glyphen in einer längst überholten Geometrie sind kein Moat-Bruch, und ein Rewrite auf einem öffentlichen main mit vier laufenden Worktrees kostet mehr, als er schützt. Das Nachweis-Netz im Lizenz-Skill (Inhalts-Pickaxe statt Endungsfilter) ist der eigentliche Fix und läuft unabhängig von dieser Antwort.

### F10. Prod-Re-Harvest der nachgefahrenen Wort-Zeilen mit Kette v5 — wann, oder ob überhaupt?

- **Warum:** Seit dem 19.08. viermal im Journal auf „hinter Owner-Go + dbsnapshot“ vertagt, ohne Todoist-Zeiger, also nur als Fließtext in einem 6.200-Zeilen-Dokument. Die gespeicherten Vorkommen stammen alle aus dem Ketten-Pfad, aber aus einem älteren Stack; ein Re-Harvest ist eine deklarierte Re-Baseline der Wort-Referenzen.
- **Optionen:** Jetzt terminieren (dbsnapshot, Lauf, §14-Nachmessung) · Nach der humanbench-Runde und LF11, damit nur einmal neu geerntet wird · Gar nicht — v5 unterscheidet sich zu wenig, Aufwand nicht gerechtfertigt
- **Empfehlung:** Nach LF11. Ein Re-Harvest, der die neuen Zeilen gleich mitnimmt, kostet einen Lauf statt zwei — bis dahin legt eine Sitzung den Todoist-Zeiger an, damit der Schritt aus dem Fließtext herauskommt.

### F11. Die beiden Agenten-Anleitungen sollen laut eigener Kopfzeile synchron sein, sind es aber nur zu 42 % — Import-Dedup oder Hand-Sync mit Pin-Test?

- **Warum:** Fünf Regeln stehen nur in copilot-instructions und erreichen Claude-Sessions nie, vier Owner-Direktiven leben nur im lokalen Gedächtnis und erreichen Cloud-Sessions und Copilot nie, und zwei Memory-Einträge widersprechen inzwischen dem Repo. Wegen der Sibling-Transfer-Regel gilt die Wahl für anyplot mit.
- **Optionen:** Variante A: CLAUDE.md = Claude-spezifische Regeln + `@.github/copilot-instructions.md`-Import (eine Quelle, kein Hand-Sync) · Variante B: anyplot-Muster beibehalten (zwei Dateien, Hand-Sync) plus `tests/test_agent_instructions.py` als Pin · Nichts ändern, nur die fehlenden Regeln in beide Dateien nachziehen
- **Empfehlung:** Variante B. Copilot Code Review löst nachweislich keine @-Importe und keine Links auf — alles, was der Reviewer wissen muss, muss physisch in seiner Datei stehen. Der Pin-Test (jeder Backtick-Pfad existiert, jeder §-Verweis trifft) fängt genau die Drift, die heute unbemerkt läuft, und kostet eine Testdatei.

## Todoist: schließbar mit Beleg

- „Website 5/8: Buchstaben-Detail auf der Schreibtafel“ (`6hPP6RHvWGf2836J`) — geliefert durch PR #457, gemergt 2026-08-30T06:01:03Z; live nachgeprüft inklusive Deep-Link `/tafel?g=k` mit Auto-Scroll.
- „Website 7/8: Übungsblatt mit Text“ (`6hPP6RQr5xqGhwfr`) — geliefert durch PR #458, gemergt 2026-08-30T06:25:12Z; die verbleibenden Mängel (stille Zeilenverluste, fehlendes `max`) sind ein NEUER Befund (Rang 3) und gehören in eine eigene Aufgabe, nicht in diese.
- „Website: Kleinkram aus dem Audit 2026-08-29“ (`6hPP6Rfx9H7xfcVr`) — geliefert durch PR #459, gemergt 2026-08-30T06:33:38Z.
- „Eigenhand: Welle 2 ist gebaut“ (`6hPPh7r2f693m8Vr`) — geliefert durch PR #449, gemergt 2026-08-29T19:58:30Z; der genannte Restschritt ist wörtlich „derselbe wie im Task ‚ersten Bogen drucken‘“, also eine Dublette.
- „Lokale .env auf die DB-Rolle `kurrentschrift` umstellen + ADMIN_TOKEN eintragen“ (`6hPHRV23c8mXRWQr`) — ohne Wertausgabe geprüft: `DATABASE_URL` parst zu user=kurrentschrift, host=34.7.236.161, db=kurrentschrift; `grep -c '^ADMIN_TOKEN=' .env` = 1; `KURRENTSCHRIFT_ARCHIVE` gesetzt und das Verzeichnis enthält `db-snapshots/`.
- „was ich gerne verwechsele f und h“ (`6hPcWqMh9GwWX7VX`) — das Paar steht bereits in `app/src/lib/lesarten.ts` (`f: ['s','h','t']`, `h: ['f']`) und im gepinnten Zwilling `core/lesarten/__init__.py`; von mir im Code nachgeprüft. Der Schwesterfall g↔p fehlt dagegen in beiden Tabellen und gehört als eigene, kleine Aufgabe angelegt.
- „Form-Abstand-Arm vorregistrieren und messen“ (`6hPJVCwCM6Wj43Fr`) — in PR #474 vorregistriert UND gemessen (τ_form 1,40, P bei p90 1,01 auf Rang 5 von 22, Negativkontrolle s 0,42, sechs Sensitivitätsvarianten drehen es nicht). ERST NACH dem Merge schließen und in derselben Bewegung durch die schmale Nachfolgeaufgabe ersetzen: „v/E/k mit τ_form = 1,40 über die Archiv-Zeilen (Snapshot 2026-08-26T23-16-40Z) nachmessen“ — sonst verschwindet der einzige offene Rest mit dem erledigten Negativ.
- „Website SEO: Seitentitel und H1 mit Suchbegriffen, dünne Hubs auffüllen“ (`6hPPXgQRXmg9VGjJ`) — NICHT schließen, sondern umschreiben: Titel, H1, Hub-Absätze und HEAD sind mit PR #451 geliefert; offen bleiben die erklärenden Absätze für /quiz und /federprobe und fünf Meta-Descriptions über 155 Zeichen (Track T10).

## Nicht geprüft

- Cloudflare ist über alle 20 Prüfer hinweg eine schwarze Kiste geblieben — keine Cache-Rules, keine Rate-Limiting-Regeln, keine Access-Policies, keine AI Crawl Control, kein Blick in die Worker-Konfiguration; alle Edge-Aussagen stammen aus Antwort-Headern von außen. Der Worker vor `/api/*` und `/pa/event` liegt zudem in KEINEM der beiden Repos, sein Routing- und Header-Verhalten ist damit weder review- noch versionierbar.
- Kein Zugriff auf die geteilte Cloud-SQL-DB: der Zustand von `lesart_forms`, die Laufform-Lücke (G/W/K/ue/F/ae/b), die aug29-Löschungen, die 13 geschriebenen LF3b-W-Zeilen und das Schreibdatum der beiden Zeilen unter dem Beweis-Boden sind nur über öffentliche Payloads erschlossen, nie gegen die Datenbank geprüft. Ebenso ungeprüft: Cloud-SQL-Rollenrechte (Migrate-Job und Laufzeit teilen sich eine SA) und alle Abfragepläne — die HTTP-Suite fährt auf SQLite, Index-Deckung ist aus models.py abgeleitet, keine EXPLAIN-Läufe.
- Secret Manager wurde nicht angefasst: Entropie, Alter und Rotationsstand von ADMIN_TOKEN, CF_ACCESS_AUD und DATABASE_URL sind ungeprüft — damit ist auch nicht belegbar, wie stark die Break-Glass-Tür an der ungeschützten `*.run.app`-URL tatsächlich ist, nur dass sie ohne Ratenbegrenzung erreichbar ist.
- Kein Docker auf dieser Maschine: die Zweistufen-Dockerfile wurde argumentiert, nie gebaut; Schichtgrößen, die 531-MB-Zahl, der Kaltstart im Container und die Wirkung des vorgeschlagenen Image-Jobs sind gerechnet, nicht gemessen. Das Bytecode-Verhältnis (≈2:1) ist lokal in WSL2 mit warmem Plattencache erhoben — die Relation ist übertragbar, die Absolutzahlen nicht.
- Kein Bench-Volllauf gegen die dokumentierte Wurzel: die lokalen Fixture-Roots tragen `exported_at 2026-08-14`, die §15-Zahlen stammen aus Cloud-Sitzungen mit der sep01-Wurzel. Ein lokaler Lauf lieferte 0,108091 (die aug26-Basis) — vergleichbar mit nichts Dokumentiertem. Kein Kettenlauf (`tracebench --candidate chain`), nur der Identitäts-Gate; InkSight Stufe 2 gar nicht (braucht das isolierte Python-3.11-TF-venv, 2,2 GB, plus 1,2 GB Gewichte).
- Die Qualität des Geschriebenen wurde von EINEM nicht-blinden Richter beschrieben, nicht bewertet: es gab keine humanbench-Runde, die Roughness-, Naht- und Strichbreiten-Kennzahlen sind Ad-hoc-Messungen dieser Prüfung und keine eingefrorenen Lineale, und der Strichbreiten-Vergleich beruht auf der binarisierten Platten-Maske (Druck-Gain und Ausbleichen nicht separiert). Nur Sütterlin-1922 geprüft — Kurrent (eigene Metrik, Schwellzug) und Offenbacher (Breitfeder) wurden nie gerendert oder verglichen.
- Keine GitHub-Schreibzugriffe: kein `@dependabot rebase` ausgelöst, keine Einstellung umgelegt, kein Issue kommentiert. Dass ein neu angestoßener Dependabot-Lauf die Ruleset-Hürde nimmt, ist nur indirekt belegt (ein if-übersprungener Job erzeugt auf main HEAD nachweislich einen Check-Run mit `skipped`); über diesen Pfad ist im Repo noch nie ein PR gemergt worden.
- Der Verlauf wurde nur auf Blob-Größen und mit `git grep` auf HEAD gegen Secret-Muster geprüft (gitleaks/trufflehog nicht installiert). Der Fund des reservierten Blobs stammt aus einer gezielten Inhalts-Pickaxe über fünf Feldnamen — andere Schlüsselwörter, andere Dateitypen und ältere Umbenennungen sind nicht abgesucht; das Restrisiko zeigt sich beim Einschalten des GitHub-Secret-Scannings.
- Admin-Schreibpfade gegen die LIVE-DB blieben ungeprüft (die in CLAUDE.md benannte bekannte Lücke): die Werkbank wurde gegen eine Wegwerf-SQLite mit fünf Buchstaben, null Vorkommen und einer Hand ohne Setup bedient. Ungesehen damit: Statistik-Panels mit echten Daten, „Schlechteste zuerst“ über ein volles Alphabet, „Laufform übernehmen“, „Alle neu ableiten“, der Nachfahr-Editor, der Paar-Editor, Eigenhand-Bogen-Druck und Streifen-Upload.
- Kein echtes Gerät, kein Querformat, kein Druck: mobil lief alles über Chrome-Device-Emulation, die Touch-Zahlen sind Geometrie statt Bedienerfahrung, iOS-Safari-Eigenheiten und die Bildschirmtastatur sind ungeprüft, und keine erzeugte PDF-Datei (Übungsblatt, Lesetafel, Eigenhand-Bogen) wurde geöffnet — der Sandbox-Browser unterbindet Downloads, belegt ist nur die Existenz des Blobs.
- Lokal Node 20 statt der geforderten 22: `npm run build` und `npm run prerender` liefen nie durch (`--experimental-strip-types` fehlt), die Bundle-Zahlen stammen aus `npx vite build`, und die Aktualität der eingecheckten Prerender-Dateien ist nur über den Vitest-Drift-Guard und einen leeren `git status` belegt, nicht über einen frischen Render.
- Rechtsfragen sind keine Rechtsberatung: die Anwendbarkeit von DDG §5 auf eine nichtkommerzielle Privatseite, die Art.-27-Vertreterfrage, die Reichweite des GPL-Weitergabebegriffs für Serverdaten und die PD-Begründungen der SOURCE.md-Dateien wurden auf innere Stimmigkeit geprüft, nicht juristisch nachrecherchiert.
- Die Worktrees unter `.claude/worktrees/` (acht Repo-Kopien, darunter der laufende PR #474) wurden weisungsgemäß nicht betreten — Befund-Kontext zu LF10/Form-Abstand kann sich damit überschneiden, und ob die übrigen sieben noch gebraucht werden, konnte niemand beurteilen.
- Nicht geprüft, weil zurückgestellt oder außerhalb: die englische Fassung der Website (#460), die Admin-Locales, `docs/schriftkunde/` inhaltlich über die Quervergleiche hinaus, die Distraktor-Qualität der 641 Quiz-Wörter, und der Issue- und Aufgabenrückstau von anyplot auf Übertragungskandidaten in Richtung kurrentschrift — obwohl die Sibling-Regel beidseitig gilt.

